# えんじいろ UI モック（frontend/app）

**PC を主にした 3 カラム構成**の UI モック。React + TypeScript + Vite。
**データは `public/data/*.json` のダミーで、backend には接続しない。**
JSON を書き換えて再読み込みすれば画面が変わる（ビルド不要）。詳しくは
[`public/data/README.md`](public/data/README.md)。

```
┌──────────┬────────────────┬──────────────┐
│ 左サイド │  中央          │ 右サイド     │
│ ホーム   │  タイムライン  │ よみもの     │
│ さがす   │  バブル詳細    │ （準備中）   │
│ おしらせ │                │              │
│ おきにいり│      [バブる] │              │
│ プロフィール│             │              │
│ せってい │                │              │
└──────────┴────────────────┴──────────────┘
```

左サイドを押すと**中央だけ**が差し替わる。`バブる` を押すと右サイドが投稿パネルに
入れ替わり、中央が左へ詰まる（中央はスクロールできたまま）。
同じパネルを返信でも使い回す。

**見た目の方向性は「よふけの バブル」**（UI刷新 2026-08-26）。地・光・輪の3層を敷いた場に、
本文の入った吹き出しが浮かんでいる、という一枚の絵として画面を組む。
角丸の長方形を縦に並べる形をやめたのは、それが一番強く「よくある SNS」に見えていたため。
詳しくは下の「UI刷新（2026-08-26）」。

正本の階層は [`../../AGENTS.md`](../../AGENTS.md) →
[`../../docs/specification.md`](../../docs/specification.md) →
[`../../docs/design_doc.md`](../../docs/design_doc.md) →
[`../../DESIGN.md`](../../DESIGN.md) の順。上が常に優先。

## 動かす

```bash
cd frontend/app
npm install
npm run dev      # http://localhost:5173
```

```bash
npm run build    # tsc -b + vite build
npm run lint     # oxlint
```

## モックの状態は URL で切り替える

画面の操作だけでは作れない状態（AI が止まっている・フィードが空）を見るための足場。
**製品の UI ではない**ので、画面の上に帯として置くのをやめ、URL のクエリに退避した
（人間の決定、2026-08-28。それまでは `components/MockControls.tsx` が画面の一番上を占めていた）。

読むのは `src/lib/mockScenario.ts` の1か所だけで、実 API に差し替えるときにこのファイルごと消える。

| クエリ | できること |
|---|---|
| `?feed=empty` / `?feed=loading` | フィードが空 / 読み込み中（skeleton）の状態 |
| `?ai-eval=off` | AI 文章評価が停止（NFR-003：投稿できない） |
| `?ai-transform=off` | AI 文章生成が停止（NFR-001：投稿は続けられる） |
| `?entry=intro` / `signup` / `login` | 入り口の画面から始める |
| `?theme=dark` / `kid` / `normal` / `system` | テーマを1回だけ選び直す（以後は保存された選択が勝つ） |

- `off` の代わりに `false` / `0` も効く。知らない値は既定に落とす（URL は未検証の入力）
- 読むのは起動時の1回だけ。URL を書き換えたら再読み込みする
- テーマの切り替えは製品側の **せってい** 画面にある。`?theme=` は見比べ用の近道
- 例：`http://localhost:5173/?ai-eval=off&feed=empty`

投稿パネルにあったモック用のボタン（「block になる例」「rewrite_required になる例」）は
削除した（人間の指示 2026-08-28）。拒否の見え方は、本文に連絡先や待ち合わせを書けば出る
（判定は `src/lib/mockModeration.ts`）。

## ファイルの置き場所

```
src/
  tokens/      3テーマの CSS 変数と、書体・動き・余白のトークン
    theme.css        ★ 色を書いてよい唯一のファイル
    typography.css   書体階層（.t-bubble-body など）
    motion.css       動きのトークンと5つの keyframes、reduced-motion
    base.css         余白・レイアウト・フォーカス・共通の地ならし
  components/  共通部品（LeftRail / RightRail / ReactionRow / BubbleCard / BubbleBody …）
    Illustration.tsx ★ イラストの出し口。テーマごとに SVG ファイルを選び替える
    BrandMark.tsx    ブランドの印（しっぽ付きの吹き出しに「…」）
    ActivityPanel.tsx ★ プロフィールの一覧（S8 / S6 共通。件数の巻き戻しもここが持つ）
  screens/     TimelineScreen / BubbleDetailScreen / ComposePanel /
               MyProfileScreen / PublicProfileScreen / FavoritesScreen /
               SettingsScreen / SignUpScreen / LoginScreen / IntroScreen /
               PlaceholderScreen
  data/        データ層
    api.ts           ★ サーバとの境界。実 API 差し替え時はここだけ触る
    mockSource.ts    public/data/*.json を読みに行くだけの層。実 API 差し替え時に消える
    types.ts         ドメイン型（accountId 相当のフィールドを持たない）
    reactions.ts     ★ 対象別リアクションの唯一の定義
  lib/         mockAiTransform / mockAiEvaluate / mockModeration / 相対時刻 /
               生年月日 / テーマ / あやすの返信規則
    mockScenario.ts     ★ モックの状態を URL から読む。実 API 差し替え時に消える
    personaStatusText.ts 「推定 …」の一行（S8 / S6 共通。FR-PROFILE-001/002）
    sootheCountText.ts   「あやす N / まだ あやされてない」（3か所共通）
    useResolvedTheme.ts ★ いま実際に効いているテーマ（normal/dark/kid）を返す。
                          <img> の SVG には CSS 変数が継承されないので、
                          「変数を渡す」のではなく「ファイルを選ぶ」ために要る

public/data/   ★ ダミーデータの実体（JSON）。書き換えると画面が変わる
public/images/ ★ イラスト（frontend/images/*.svg のコピー）。テーマごとに3ファイル
```

差し替えるときに触る場所は 2 つだけ：

- **API 接続**：`src/data/api.ts` の各関数の中身を実 API への `fetch` にする。
  `src/data/mockSource.ts` と `public/data/` はそのとき消える。画面側は触らない
- **配色の確定**（DESIGN.md §2.2 のノーマルが未確定）：`src/tokens/theme.css` の
  素の `:root` の値。コンポーネントに hex は 1 つも無い

## UI刷新（2026-08-26）

### 何を変えたか

| ところ | 前 | 後 |
|---|---|---|
| 画面の地 | `--canvas` の一色 | 地 → 光（`--aura-*` の放射グラデ）→ 輪（細い円4つ）→ 中身 の4層。**動かさない**（`position: fixed`） |
| 列の仕切り | 中央の列の左右に 1px の縦線 | 縦線を廃止。左右を「島」（面）にして、中央を余白で切る |
| バブルカード | 角丸の長方形。上にニックネーム、下に本文 | **しっぽ付きの吹き出し**。中に本文、しっぽの先に書いた人、その下にリアクション |
| タイムラインの先頭 | 「ホーム」の見出しだけ | 口上（呼びかけ1行）を主役として置く。送れば流れて消える |
| Intro | 説明カード1枚 | 表紙（名乗り・ひとこと・イラスト）＋説明カードの2面 |
| 投稿パネルの入力欄 | 角丸の箱 | 吹き出しの形。空のときだけ中にイラストが出て、書き始めると消える |
| 文字数カウンタ | 数字だけ | ふくらんでいく輪 ＋ 数字（DESIGN.md §4 Meter の「バーだけで伝えない」に合わせる） |
| リアクション | 角丸（`--radius-control`） | 丸（`--rounded-pill`）。確定操作のボタンと形で分ける |
| 完了のしらせ・AI 変換案 | 角丸の箱 | どちらも吹き出し。画面から出る「ことば」を同じ形にそろえる |

追加したトークン（`tokens/theme.css`、どれも既存の役割名から `color-mix` で作る。生の hex は増やしていない）：

- `--aura-warm` / `--aura-cool` … 光の層の色
- `--field-ring` / `--field-ring-width` … 輪の色と太さ
- `--radius-balloon` / `--balloon-tail-size` / `--balloon-tail-inset` … 吹き出しの形
- `--radius-reply` … 返事（あやす）の形。吹き出しと左右を反転させる

**3テーマの差は色だけではない。** 光の量・輪の太さ・吹き出しの角の形をテーマごとに変えている
（園児UI は輪が 2 倍太く、吹き出しが不定形）。骨格（余白・書体・動き・コンポーネントの構造）は
DESIGN.md §1.2 のとおり共通のまま。

### イラストの使い分け

素材は `frontend/images/` の 3 種 × 3 テーマ ＝ 9 ファイル。**全部使っている**が、
画面ごとに役割を分けていて、同じ絵を全画面に出さない。イラストを置かない画面のほうが多い。

| ファイル | どこで | なぜ |
|---|---|---|
| `soft-bubble-orbit-*.svg` | **Intro の表紙（主役）** | 「めぐるバブル」。吐き出したものが受け止められて戻ってくる、というこのサービスの主張がそのまま絵になっている。表紙は説明ではなく世界観を出す場所なので、いちばん強い絵をここに置いた |
| `soft-bubble-haven-*.svg` | **空状態の主役**（タイムラインが空 / おきにいりが空） | 「ひとやすみのバブル」。静かで、何も無いことを責めない絵柄。空状態は「まだ無い」を伝える場所なので、余白の中に置ける絵が要る |
| `soft-bubble-drops-*.svg` | **投稿パネルの入力欄の中**（本文が空のときだけ） | 「しずくのバブル」。上に落ちるしずくが「吐き出す」に直接つながる。書き始めると `opacity` だけで消える（要素は残すので高さが跳ねない） |

置かなかったところ：タイムライン本体、バブル詳細、あやす詳細、プロフィール、ログイン・登録。
タイムラインは**吹き出しが並んでいること自体**が絵なので、そこへイラストを足すと主役が2つになる。
詳細とプロフィールは読むための画面で、絵を置く場所は本文の面積を削る。

**`<img>` で読み込んだ SVG に、アプリ側の CSS 変数は継承されない。**
素材側は `var(--illustration-fill, #F4C7B8)` のようにフォールバック色を持っていて、
外部読み込みではそのフォールバックが効く。だから色を渡すのではなく
`lib/useResolvedTheme.ts` で**ファイルを選び替えている**（`components/Illustration.tsx`）。
判定材料は `<html data-theme>` と `prefers-color-scheme` の2つで、
`tokens/theme.css` の実装契約（DESIGN.md §8.3）と同じ順序。

イラストはすべて装飾なので `alt=""` ＋ `aria-hidden="true"`、`pointer-events: none`。
意味は隣の文字（口上・空状態のコピー・placeholder）が持つ。

### 増やした依存

**無し。** 背景の層・吹き出しのしっぽ・カウンタの輪・出入りの動きは、すべて CSS と既存トークンで作っている。
shadcn/ui・Tailwind・Motion のいずれも入れていない。

## 配色と操作感の申し送りへの対応（2026-08-26）

[`../../docs/color_and_ui_findings.md`](../../docs/color_and_ui_findings.md) の
**§0「決定ごと」（人間の決定 2026-08-26）を実装した。**
同 §7「決めてほしいこと」のうち、コンセプトの判断が要るものには手を付けていない（下の表）。

**申し送りそのものは編集していない。** あれは別インスタンスの成果物かもしれないので、
実装状況はこちら（自分の担当ファイル）に書いている。

### 実装したもの

| 申し送り | やったこと | ファイル |
|---|---|---|
| §0-1 ダークの地を紺にする | `canvas #11151C` / `surface #161B24` / `card #1C222E` / `hairline #262E3E`。**明度の段（8.8 / 11.4 / 14.5 / 19.6%）は据え置き、色相と彩度だけ替えた。**文字と臙脂の値は変えていない | `tokens/theme.css`、`DESIGN.md` frontmatter・§2.3 |
| §1 夜空の光 | `--aura-cool` を `mother`（緑）由来から `hairline` 由来へ。紺に緑の光を置くと「暗い森」になる。輪（`--field-ring`）も同じ理由で空の側にした | `tokens/theme.css` |
| §0-2 赤ちゃんが主役（輝度） | ダークの `mother` を `#7FC79A`（9.18:1）→ `#4AAA6E`（6.33:1）。赤ちゃん 7.48:1 との差 **+1.15**（赤ちゃんが前） | `tokens/theme.css`、`DESIGN.md` |
| §0-2 赤ちゃんが主役（面積） | **あやすカードの地と枠を役割色で塗るのをやめた。**役割はアバター「母」＋バッジ「お母さん」が示す（`DESIGN.md` §2.5 の二重化は満たしたまま） | `components/SootheItem.css`、`screens/SootheDetailScreen.css` |
| §2 主役の序列を判定条件にする | `DESIGN.md` §2.2 に条件6「`mother` は `canvas` 上で `accent-line` より低いこと」を追加 | `DESIGN.md` §2.2・§2.6 |
| §0-4 カーソルの位置 | `:hover` が無かった **リアクション・セグメント・あやす本文・入力欄**に追加。規則は「`--ink` を 8% 重ねて1段持ち上げ、枠を `accent-line`」の1つだけ | `tokens/base.css`（`--hover-veil`）、`ReactionRow.css`、`SegmentedTabs.css`、`SootheItem.css`、`ActivityItem.css`、`TextField.css`、`BubbleCard.css` |
| §4 ホバーの当たり判定 | 擬似要素で当たり判定を広げている部品（あやす1件・活動一覧の1件）は、**ボタンではなく擬似要素のほう**を光らせる。ボタン側を光らせると押せる範囲と光る範囲がずれる | `SootheItem.css`、`ActivityItem.css` |
| §0-3 主操作の動き | 「バブる」の押下を `scale(0.985)` → `scale(0.97)`。送信ボタンは**送れる状態に変わった瞬間だけ一度 pop**（§5 の「押す前の誘い」案2）。完了トーストに波紋（既存の `eg-ring-out` を使い回し） | `App.css`、`ComposePanel.tsx/.css`、`Toast.css` |
| §6 `kid` の `on-accent` が文書に無い | `DESIGN.md` frontmatter に1行追加（実装は前から `#FFFDF9` を持っていた） | `DESIGN.md` |
| §6 §7.5 の reduced motion が古い | `* { animation-duration: 1ms }` ではなく「トークンを 0 と 1 に潰す」実装のほうが正しいので、文書を実装に合わせた | `DESIGN.md` §7.5 |

### 実装していないもの（判断が要る）

| 申し送り | なぜ手を付けていないか |
|---|---|
| §2-B お母さんの色相を藤 `#AC8DB3` へ | **「お母さん＝緑（安心・やさしさ）」という意味づけを捨てる判断**になる。色ではなくコンセプトの話なので、人間の決定が要る（申し送り §7-2 も同じことを書いている）。輝度と面積だけ手当てした結果、いまの見え方でどこまで足りているかは実機で判断してほしい |
| §7-3 `mother` と `success` を別の色にする | ダークは `mother` を下げた副作用で**別の色になった**が、**ノーマルはまだ同じ hex**（`#39704E`）。`success` はどの画面でも未使用なので衝突は表に出ていない。ブランド色の変更は人間の判断 |
| §2 園児UI の主役の差が +0.48 と薄い | 明るい地では「暗いほど目立つ」ので、差を広げるには `mother` を**明るく**する必要がある。いまの `#2E7A50` は canvas 上 4.96:1 で、下限 4.5:1 まで余裕が 0.46 しかない。可読性を割らずに詰める余地がほとんど無い |
| §1 ダークの文字を冷やすか | 紺の上で生成り `#F4EDE4` が黄ばんで見えるかは、**実機で見ないと決められない**（申し送りも同じ判断）。いまは暖色のまま |
| §5「押す前の誘い」の案1・案3 | 誘いは1つに絞った（案2＝送れる状態に変わった瞬間の pop）。複数入れると `DESIGN.md` §7.2「常時動く背景は目を疲れさせる」と衝突する |
| §6 `engiiro-crayon-logo.png`（透過なし・1.5MB） | `frontend/color-check/` の資産で、app は使っていない。ブランド素材として使う予定があるかは人間に確認が要る |

## 5つの指摘への対応（2026-08-27）

人間から直接受けた指摘5件。

### 1. 「バブる」が主役に見えない → 大きさ・登場・誘い・反応の4つを重ねた

> 一番押してほしいボタンはバブルなので、そこに対してアクションやアニメーションを追加してください。

| 何を | どうした |
|---|---|
| 大きさ | `--fab-height` を新設（56px）。ほかのボタンは 48px のまま。**色と位置だけでなく大きさでも主役**にする |
| 登場 | 画面に着いたとき、下から一度だけ浮かび上がる（`eg-fab-in`。`modal` 型の使い回し） |
| 誘い | そのあと輪が2回ひろがって**止まる**（`eg-ring-out` の使い回し）。繰り返さない |
| 反応 | カーソル／フォーカスで小さなバブルが3つ立ちのぼる（`eg-fab-drift`）。押すと `--press-scale-strong` で縮む |

- 型は `tokens/motion.css` に `bubble-call` として1か所にまとめた。`DESIGN.md` §7.3 の表も
  「5つ」→「6つ」に直してある
- **常時は動かさない**（§7.2）。誘いが終わったあと、動いているアニメーションが 0 件であることを
  3テーマ × 4幅で実測した
- **キーボードでも同じものが見える**（`:focus-visible`）。マウスを持っている人にだけ反応する形にしない
- reduced motion では輪と立ちのぼりを `display: none` にする。移動の原資が 0 に潰れると、
  その場で点滅するだけの意味の無い動きになるため。消しても失われる情報は無い

### 2. 「バブる」が最下部にあり、下までスクロールしないと見えない

原因はボタンの側ではなく**外側の骨組み**にあった。

- 上にモックの操作帯（`.eg-mock`、78〜206px）が積まれていた（2026-08-28 に URL クエリへ退避して削除）。
  中央の列は `100vh` で作られていたので、**列の下端＝ viewport の下端 ＋ 操作帯の高さ**。
  そこに貼りついたボタンは最初から画面の外に居た（1440×800 で `top: 807px`）。
  帯が無くなっても下の直し方はそのまま残している。下端に貼りつくものが増えたときに同じことが起きる
- 直し方：`.eg-app--shell` を縦に積む箱にして高さを `100dvh` に固定し、余りを `.eg-layout` に配る。
  さらに `grid-template-rows: minmax(0, 1fr)` で行の高さを**決まった値**にした。
  ここが `auto`（＝決まっていない）だと、中の `.eg-center` が書いている `100%` が `auto` に化けて効かない
- 併せて `.eg-rail` と `.eg-compose` の `100vh` を `100%` にした。viewport から取ると、
  上に積まれたぶんだけ下へはみ出すため
- **ボタンを中央の列の外へ出した**（人間の決定 2026-08-27「常に表示されている状態を優先」）。
  列の中に居るということは「読みものの流れの一部」ということでもあり、キーボードだけで
  操作する人はカードを全部通り過ぎてからしか届かなかった。
  DOM 上は左サイドの直後に置き、見た目の位置は grid の列指定（2列目・下端・右寄せ）で決める。
  製品側のタブ順は **ホーム → さがす → おしらせ → おきにいり → プロフィール → せってい →
  バブる → あたらしくする → 本文 → 書いた人 → リアクション**。
  見た目の位置・逃げ幅（右 28px / 下 24px）・フィード幅 600px は前と1px も変えていない
- 列の末尾には中身の無い `.eg-fab-slot` だけを残した。ボタンぶんの高さを確保するためのもので、
  これが無いと いちばん下まで送りきったとき最後のカードがボタンの下に隠れる
- 結果：PC ではページ自体が動かなくなり（`pageScroll: 0`）、左右の列も「バブる」も常に見えている。
  767px 未満はこれまでどおりページを送る（ボタンは `fixed`）
- **入口の画面（説明・登録・ログイン）にはこの固定を効かせない**（`--shell` を付けていない）。
  あちらは1枚の紙が縦に伸びる作りで、高さを固定すると背の低い画面で下が読めなくなる

### 3. 「もどる」がホームに着地する → 通ってきた道を1本の配列にした

> おきにいり → アカウントページ → バブル のあと、もどるとホームへ行ってしまう。

- 原因：`view`（左サイドの選択）／`detailStack`（詳細の積み重ね）／`publicBackTo`（S6 の戻り先）の
  **3つを別々に持っていた**。プロフィールからバブルを開くとき、途中で `view` を `timeline` に
  書き換えていたので、来た道がどこにも残らなかった
- 直し方：`Location`（`view` / `public` / `bubble` / `soothe`）の配列 1本にまとめた。
  中央に出せるものは、左サイドの画面も公開プロフィールも詳細も、等しく「場所」として1段積む。
  「もどる」は種類を問わず1段外すだけで、戻り先を覚える変数は要らなくなった
- 底は常にホーム。**左サイドを押したときだけ**積み上げを捨てて底から始め直す
- 着いた先は毎回引き直す（進むときも戻るときも同じ `enter()` を通す）。潜っているあいだに
  増えたリアクションやあやすが、戻った瞬間に消えたように見えないため
- 実測：`おきにいり → 公開プロフィール → バブル → もどる → 公開プロフィール → もどる → おきにいり`。
  `ホーム → バブル → あやす → もどる → バブル → もどる → ホーム` も同じ

### 4. 「よんだ／まだ」を廃止した

> 実装依頼をしていないかと思います。バックエンド側に送る API も無いだろうし。

- 仕様書・設計書のどちらにも「既読」は**1語も無い**（`grep` で確認）。送り先の API も無い
- 画面だけが覚えている「よんだ」は、別の端末で開けば嘘になる。本人にしか意味が無い情報のために、
  カード1枚あたり1行ぶんの高さを使っていた
- 型（`Bubble.read`）・モックの種（`BubbleSeed.read`・`bubbles.json`）・`markRead()` ごと落とした。
  画面から消すだけにすると、次に触る人が「送り先があるはず」と思い込む
- 詰めたぶん：2段目（書いた人の行）から右端の印が消え、3段目（リアクション行）との間を
  `--space-xs` → `--space-xxs` に近づけた。1枚が1つの塊に見える
- **失われた情報は無い。** 誰が・いつ・どれだけあやされたかは、吹き出しの中と2段目にそのまま残る

### 5. 登録画面に生年月日の欄を追加した

- S8 本人専用プロフィールには**前から生年月日が出ていた**のに、入れる場所がどこにも無かった
- パスワードとニックネームのあいだに置いた。`type="date"`、`min` は 1900-01-01、`max` はきょう
- **これはアカウントの情報で、ペルソナの情報ではない。** 公開プロフィール（S6）の型にこの項目は無く、
  AI へ渡す入力にも入らない（FR-PRIV-003/004）。そのことを入力欄のすぐ下に書いてある
  （「自分のプロフィールにだけ 出ます。ほかの人からは 見えません」）。
  ここで黙って受け取ると、なぜ要るのか分からないまま個人の情報を書かせることになる
- 年齢で入会を断る判定は**入れていない**。年齢制限は仕様書に無く、決めるのは人間（Issue #7 と同じ扱い）
- 併せて `tokens/theme.css` に `color-scheme` を足した。これが無いと、ブラウザが自前で描く
  カレンダーの印がダークで黒いまま出て地に沈む。スクロールバーと選択文字の地も同時に揃う

## リアクションの2件の指摘への対応（2026-08-27）

人間から直接受けた指摘2件。原因はどちらも同じで、**リアクションを押すと詳細画面を
skeleton に差し替えて引き直していた**こと（`App.tsx` の `refresh` → `loadDetail`）。

### 1. 5回目の演出が一瞬すぎて見えない

3つ重なっていた。

- **押したボタンごと画面が外れていた。** 引き直しのあいだ `BubbleDetailScreen` を
  skeleton に差し替えていたので、演出は始まった直後に DOM ごと消えていた
  → `loadDetail(ref, { quiet: true })` を足し、引き直しでは skeleton を出さない
- **演出の強さがサーバの返事待ちだった。** 押した回数が反映されるのはモックの 520ms 後で、
  それまで data-step は「4」のまま。5回目の合図は pop が終わりかけてから始まっていた
  → 押した時点で回数を決める（`ReactionRow` の `popping.step`）。水位も同じ数から作るので、
  合図と地の満ち方がずれない
- **終わりを `animationend` で決めていた。** 連続で押すと pop は走ったままなので、
  最後の押下より早く終わりが来て演出を切っていた
  → 見込みを捨てる合図はサーバの返事にし、返事が来ないときだけ 2 秒で捨てる
- あわせて合図そのものを長くした（粒 3 → 5、40ms ずらし、最後の粒まで 660ms）

### 2. 詳細でリアクションすると、一瞬前の画面に戻ってロードされる

同じ原因。skeleton へ差し替えていたので、520ms のあいだ「読み込み中の別の画面」に見え、
戻ってきたときにスクロール位置も頭に戻っていた。引き直しを quiet にして直した。
タイムラインは前から `loadFeed()` を静かに呼んでいたので、この症状は詳細だけで出ていた。

## 全体の見直し（2026-08-28）

人間の指示で全体を読み直した。直したものは次の15件。

### 直した不具合

| # | 何が起きていたか | 直し方 |
|---|---|---|
| 1 | **引き直しで画面が skeleton に戻る。** リアクション・投稿・削除のあと、プロフィールや大好きの一覧まで中身が消えていた。詳細画面で直したのと同じ形の問題 | 読み込み関数に `quiet` を足し、着いたとき（`enter`）だけ skeleton を出す。引き直し（`refresh`）と大好きの解除では出さない |
| 2 | **文字数が読み上げから読めない。** `134 / 150` に `aria-hidden` が付いていて、画面を見ない人には 150 を超えるまで文字数が分からなかった | `aria-hidden` を外し、`.eg-sr-only` で「つかった文字」を添えた。live ではないので打つたびには読まれない |
| 3 | **ゲストの見え方を画面側で作り直していた。** `isMe` / `liked` を `App.tsx` の描画時に落としていた | `data/api.ts` の `fetchPublicProfile` が閲覧者に合わせて返す。境界の外に出た値を画面が直す形をやめた（FR-AUTH-001 の立場） |
| 4 | **定義の無いトークンを参照していた。** `MockControls.css` の `var(--nav-width-desktop)` はどこにも定義が無く、`padding-left` が黙って捨てられていた | 帯ごと削除（URL クエリへ退避）。あわせて `App.css` の「操作帯があるから」という説明も経緯として書き直した |
| 5 | **誰も渡していない prop。** `SootheItem` の `replyToNickname` は2つの呼び出し元のどちらも渡しておらず、分岐と CSS が生きていなかった | prop・分岐・`.eg-soothe__quote` を落とした |
| 6 | **説明と実装がずれた注釈。** `STAMP_GROUP_FALLBACK` は「タブの初期値に使う」と書いてあるが使っていない。`ReactionRow` の説明は粒が3つのまま（実装は5つ） | export をやめて中だけで使う形にし、注釈を実装に合わせた |
| 7 | **取り残された説明。** `guard` の説明が、間の関数が挟まって `applySession` の上に付いていた | `guard` へ戻した |

### 寄せた重複

| # | どこにあったか | 寄せた先 |
|---|---|---|
| 8 | 削除の確認の文言が S4 と S8 に同じものが2つ | `components/ConfirmDialog.tsx` の `BubbleDeleteConfirm`（文言つきの部品として出す） |
| 9 | 「まだ だれも あやしていません」が S4 と S4b に2つ | `components/EmptyState.tsx` の `NoSootheState` |
| 10 | 「あやす N / まだ あやされてない」が3か所 | `lib/sootheCountText.ts` |
| 11 | 「推定 …」の組み立て（月齢から年を出す割り算）が S8 と S6 に2つ | `lib/personaStatusText.ts`。材料が無いときの文言だけ画面から渡す |
| 12 | プロフィールの一覧（skeleton → 空 → 一覧 → さらに よみこむ）が S8 と S6 に同じ40行。CSS も `__panel` / `__list` / `__more` が複製 | `components/ActivityPanel.tsx`（＋ CSS 1本）。件数の巻き戻しも部品が持つので、画面側の `setVisible(PAGE_SIZE)` が消えた |
| 13 | `reactToBubble` / `reactToSoothe` がほぼ同じ。上限の「5回」も文言に直書き | 1つの `react(input)` にまとめ、`REACTION_MAX_PER_USER` から出す |
| 14 | `MockControls` の呼び出しが `App.tsx` の2か所に同じ props で並んでいた | 帯ごと削除 |
| 15 | モックのつまみが画面の一番上を常に占めていた | URL クエリへ退避（上の節） |

### 見たが直していないもの

- **`fetchStamps()` を誰も呼んでいない。** スタンプ一覧は `STAMP_CATALOG` を直接読んでいる
  （本文の文字数の数え方も同じ）。`api.ts` を通す原則からは外れるが、
  文字数を数えるたびに await が要る形にするほうが害が大きいので、そのままにしている。**要確認**
- **`addReaction` だけモックの遅延（520ms）が無い。** 他の書き込みは全部入っている。
  リアクションは押した瞬間に返るほうが自然なので、あえて揃えていない
- **`--success` / `--warning` / `--blur-small` が未使用。** トークンの段として置いてあるもの

## スタンプ（2026-08-28）

**押すと本文に絵が入る。** 以前は `components/BubbleBody.tsx` の中に線だけの仮図形を置いていたので、
挿入しても絵に見えなかった（人間の指摘）。

### 絵柄

`frontend/images/stamps/` にある候補のうち、**「ベタ塗りシンプル」（接頭辞 `06_` / `16_`）だけ**を採った。
同じキャラ・同じ線幅・透過 PNG で揃うのがこの一族だけで、絵柄が混ざると本文の中で喧嘩する。

| 棚 | id | 名前 | 元ファイル（`frontend/images/stamps/`） |
|---|---|---|---|
| よわね | `naku` | ないちゃう | `crying/16_flat-simple_crying.png` |
| よわね | `akubi` | あくび | `greeting/06_flat-simple_good-morning.png` |
| よわね | `hoshii` | ほしい | `gimme/16_flat-simple_gimme.png` |
| うれしい | `banzai` | ばんざい | `joy/06_flat-simple_joy.png` |
| うれしい | `niko` | にこにこ | `joy/16_flat-simple_happy.png` |
| うれしい | `suki` | すき | `love/06_flat-simple_love.png` |
| よしよし | `wakaru` | わかる | `empathy/16_flat-simple_exactly.png` |
| よしよし | `ganbare` | がんばれ | `encourage/06_flat-simple_cheer-up.png` |
| よしよし | `arigato` | ありがとう | `thanks/06_flat-simple_thanks.png` |
| へんじ | `ryokai` | りょうかい | `roger/06_flat-simple_roger.png` |
| へんじ | `bikkuri` | びっくり | `surprised/06_flat-simple_surprised.png` |
| へんじ | `gomen` | ごめんね | `sorry/06_flat-simple_sorry.png` |

書き出しは3手で、素材を差し替えるときも同じことをすればよい（Pillow を使った使い捨てスクリプト）。

1. 透明の余白を落とす（トリム）… 12枚の大きさをそろえる
2. 128px の正方の中央に置く … 表示は 32px / 52px なので3倍前後
3. `optimize` で保存 … 1枚 200〜400KB → **合計 237KB / 12枚**

- 元ファイル名（`06_` / `16_`）は素材の分類で、**棚の名前とは一致しない**。
  「おはよう」の絵は大あくびで伸びをしているので、このサービスでは `akubi`（よわね）に置いた
- 採らなかった一族：リアル写真風（`03` / `07`〜`13`）、水彩（`04` / `18`）、3Dぬいぐるみ（`05` / `19`）、
  ピクセル（`25`）ほか。**どれも「その気持ちだけ」しか無く、12個をひとつの見た目で揃えられない**。
  棚を増やすなら、まず同じ一族の絵を足すほうが先

### 入力欄では文字、出したあとに絵

textarea の中に画像は出せない。押して入るのは `:naku:` という**目印の文字**で、絵になるのは表示側。
そのままだと「文字が入っただけ」に見えるので、**本文にスタンプがあるときだけ**、
入力欄の下に「こう 出ます」を出している（`containsStamp` で判定）。

contenteditable の入力欄にすれば入力欄の中に絵を出せるが、日本語の変換（IME）・カーソル・
貼り付けの掃除・文字数の数え方が全部作り直しになるので採っていない。**要相談**。

### サイズ（DESIGN.md §11 で未定だった項目）

| 場所 | 大きさ | トークン |
|---|---|---|
| 本文の中 | 32px（本文 16px / 行間 1.9 ＝ 行の高さ 30.4px にほぼ収まる） | `--stamp-size` |
| 一覧 | 52px | `--stamp-size-picker` |

下地は敷かない。絵の側が濃い輪郭と明るい塗りを持っているので、ノーマル・ダーク・園児UI の
どの地の上でも形が残る。敷くと、絵の中の白と二重の面に見える。

## 園児UI で気をつけること

園児UI は `--radius-control` が**丸薬型（999px）**になる（DESIGN.md §1.3。装飾がこのテーマの本体）。

- **高さが伸びる箱に `--radius-control` を当てない。** 角の曲線が本文を食う。
  600px 幅・170px 高の箱では半径が 84px まで育ち、`overflow: hidden` と重なると文字が切り取られる
  （実際に「せってい」の説明が園児UI だけ読めなくなっていた。人間の指摘 2026-08-28）
- 伸びる箱・面は `--radius-blob`、高さの決まった操作（ボタン・入力欄・1行の行）は `--radius-control`
- ほぼ正方形の札に丸薬型を当てると円になる。中の文字が枠からはみ出す

## 仕様のうち、UI で担保している要点

| 仕様 | どこで |
|---|---|
| 対象別リアクション（バブル・赤ちゃんあやす=3種／お母さんあやす=`ばぶー`のみ） | `data/reactions.ts` の表 → `components/ReactionRow.tsx`。画面ごとにボタンを並べ直さない |
| 1種類につき5回まで（人間の決定） | `data/constants.ts` の `REACTION_MAX_PER_USER`。**水位**（ボタンの地が下から満ちる。5回で満タン）で回数を見せ、6回目は `data/api.ts` が `max_reached` で弾く |
| スタンプを本文に挿入（FR-POST-005 / FR-STAMP-001） | `:naku:` の目印で本文に埋め、`components/BubbleBody.tsx` が絵（`public/images/stamps/<id>.png`）に置き換える。一覧は棚別のタブ（`data/stampCatalog.ts` の `STAMP_SHELVES`）。押すと本文のカーソル位置に入る |
| 赤ちゃん度・お母さん度（FR-AI-EVAL-001〜004） | `lib/mockAiEvaluate.ts`。軸が別であることを画面にも書く。結果で投稿を止めない |
| リアクションにペルソナの要素は無い（人間の決定） | `ReactionState` にペルソナの別を持たせていない。「赤ちゃんとして押す」という区別がない |
| 自分のバブル・自分のあやすにはリアクションしない（人間の決定） | `ReactionRow` の `readOnly`。押せるボタンを出さず、付いた数だけを見せる。`data/api.ts` でも弾く |
| 3種目は「まんま」（§3.1・§9.1、2026-08-25 の PO 改訂） | 旧称「わかるわぁ」「哺乳瓶」と同じ1つのリアクション。ボタンを増やさない（FR-REACT-009） |
| お母さんあやすへの返信は赤ちゃんのみ（FR-COMMENT-005） | `lib/soothePersonaRule.ts`。ペルソナ変更ボタンを**出さない**（人間の決定 2026-08-25） |
| バブルはお母さんで投稿できない（FR-POST-003） | 投稿時はペルソナ変更を `disabled` で**残す**。ラベルに理由を書く |
| タグは無し（FR-POST-004 は 2026-08-25 にコメントアウト） | タグ UI とデータを削除済み |
| 本文 150 文字（FR-POST-002 / NFR-005） | 入力は止めず、投稿ボタンだけ無効。カウンタは残り20から色が変わり、超過は負数 |
| モデレーション拒否の表示（FR-MOD-033/034） | `components/NoteBox.tsx` の `MODERATION_REJECT_TEXT`。理由コードを渡す口を作っていない |
| AI 結果を自動保存しない（FR-AI-TRANS-006） | 「これで書く」は本文欄に入れるだけ |
| 閾値を超えないと投稿できない（FR-AI-EVAL-007、改訂後） | 保存時に `data/api.ts` が評価して `evaluation` で弾く |
| AI 評価が使えないときは投稿を妨げる（NFR-003、改訂後） | 送信ボタンを無効にし、理由を出す |
| ペルソナ非連結（FR-PERSONA-003/004） | ダミーデータに `accountId` 相当が無い。1画面に自分の両ペルソナを出さない |
| 存在させない UI（16章） | フォロワー数・一覧／通報／DM／URL の自動リンク化／絶対時刻を置いていない |

## 確認のしかた

`npm run dev` で開いて、上の URL クエリを付け替えながら見る。実装時に通した確認は次のとおり。

### 1. 3テーマ × 画面

1440×900 の Chrome で 3 テーマを目視。ダークで臙脂のボタンが地に溶けていないこと
（ダークの主ボタンは `--accent-fill` `#B54254`。臙脂 `#9B3342` は暗い地で 2.54:1 しか出ないため使わない）。

### 2. コントラスト実測

配色を変えたので測り直した。**手写しではなく、ブラウザが実際に描画している値**
（`getComputedStyle` で `var(--*)` を解決したもの。`color-mix` も解決される）を取り、
3テーマ × 19 組を計算している。

| テーマ | 条件を割った組 | 主役の序列（赤ちゃん vs お母さん） | 面の段 |
|---|---|---|---|
| ノーマル | 0 | 6.77 vs 5.54 → **+1.23** | OK |
| ダーク（紺） | 0 | 7.48 vs 6.33 → **+1.15** | OK |
| 園児UI | 0 | 5.43 vs 4.96 → **+0.48** | OK |

**ダークの逆転（前は −1.68）が解消した。** 園児UI の +0.48 は薄いままで、
可読性の下限があるため詰められない（上の「実装していないもの」）。

もっとも余裕が無いのは園児UI の次の 3 組。**配色を触るときはここから壊れる。**

| 組み合わせ | 比 |
|---|---|
| `mother` `#2E7A50` on `mother-soft` `#E4F3DC` | 4.52:1 |
| `accent-line` `#C22648` on `accent-soft` `#FFE0E6` | 4.65:1 |
| `muted` `#7A675C` on `mother-soft` `#E4F3DC` | 4.63:1 |

#### 規約が無く、判断が要る組み合わせ（独立監査で指摘）

`--hairline` の 1px 枠線と、`--surface` と `--canvas` の差は、DESIGN.md §2 に規約がない。
実測すると WCAG 1.4.11（部品の境界 3:1）を3テーマとも下回る。

| 組み合わせ | ノーマル | ダーク | 園児UI |
|---|---|---|---|
| `hairline` vs `canvas` | 1.26:1 | 1.41:1 | 1.56:1 |
| `hairline` vs `surface` | 1.30:1 | 1.32:1 | 1.64:1 |
| `surface` vs `canvas` | 1.04:1 | 1.07:1 | 1.05:1 |

面と地がほぼ同じ明るさなので、入力欄やタグの輪郭は実質 1px の線だけが頼りになる。
**値を勝手に直していない。** `hairline` は DESIGN.md が役割ごとに決めた色で、
ダークと園児UI の配色は「確定候補（実測済み）」とされている。パレットの変更は
見た目の正本を持つ人間の判断なので、Issue に出して待っている（§2.2 の
「配色を差し替えるときの条件」に6番目を足す提案を含む）。
`prefers-contrast: more` では `hairline` を `muted` へ寄せる退避があるが、既定では効かない。

### 3. 幅ごとの畳み方

| 幅 | 見え方 |
|---|---|
| 1200px 以上 | 3カラム（左 220 / 中央 600 / 右 320）。パネルを開くと右が 440 になる |
| 768〜1199px | 右サイドを畳み、左サイドはアイコンだけの細い列（72px）。パネルは画面に重なる |
| 767px 以下 | 1カラム。左サイドは下部のバー。バブるボタンは右下に固定 |

1440 / 1200 / 1000 / 700 / 375px のいずれでも横スクロールなしを確認済み。
刷新後は 1440 / 960 / 768 / 375px を Chromium（Playwright）で撮って目視し、
3テーマ × タイムライン / Intro / 投稿パネル / 空状態 / 読み込み中 /
文字数超過 / AI 停止中 / AI 変換 / バブル詳細 を確認した。
コンソールエラー・画像の読み込み失敗はいずれも 0 件。

### 4. キーボードだけで ホーム → 詳細 → 返信 → 送信

Tab でバブルカードに到達（フォーカスは `2px solid var(--accent-line)`）→ Enter で詳細 →
Tab であやすボタン → Enter でパネル（開いた直後のフォーカスは本文欄）→ 入力 →
Shift+Tab で送信 → Enter。到達・送信まで確認済み。Escape でパネルが閉じる。

### 5. prefers-reduced-motion: reduce

移動・拡大・跳ね・ぼかしの原資になっているトークン（`--distance-*` / `--scale-modal` /
`--press-scale` / `--pop-scale` / `--blur-*`）を 0 と 1 に潰し、`--ease-bounce` を
`--ease-smooth-out` に差し替える。`opacity` と色の変化は時間そのまま残る（`--duration-fast` は 250ms のまま）。
粒子（`.eg-burst`）は等価物が無いので `display: none`。

### 6. grep での自己点検

```bash
# 色の直書き（tokens/theme.css 以外に hex / rgb / hsl が無いこと）
grep -rnE "#[0-9a-fA-F]{3,8}\b|rgba?\(|hsla?\(" src --include=*.css --include=*.tsx --include=*.ts \
  | grep -v "^src/tokens/theme.css"

# transition: all（コメント以外に無いこと）
grep -rn "transition:\s*all" src

# 絶対時刻の整形（無いこと。相対表示は lib/relativeTime.ts だけ）
grep -rnE "toLocale(Date|Time|)String|Intl\.DateTimeFormat" src

# accountId（コメント以外に無いこと）
grep -rni "accountid" src
```

## 既知の制限・未確定

実装しなかったこと、判断を人間に残していることの一覧。

- **左サイドの「さがす」「おしらせ」「おきにいり」「プロフィール」「せってい」は準備中の枠だけ。**
  `docs/design_doc.md` §4.1 の画面一覧（S1〜S8）に無い画面なので、中身を勝手に決めていない。
  設計書の画面一覧の更新は backend / 設計担当の領域なので触っていない
- **S1 / S6 / S7 / S8 は対象外。** 行き先のない導線は置いていない
  （ニックネームのタップで S6 へ行く導線も、S6 が無いので作っていない）
- **リアクションの取り消しは用意していない。** 「5回まで押せる」という決定だけを実装しており、
  押し間違いを戻す手段が無い。要否は要確認
- **「バブる」の焦点の順序は解決済み**（人間の決定 2026-08-27）。
  列の外に出して左サイドの直後に置いた。見た目（右下）と DOM の位置は違うが、
  このボタンは送っても動かないので読みものの順に属さない、という整理で通している
- **生年月日に年齢の下限を設けていない。** 日付として成り立つかだけを見ている（Issue #7 と同じ扱い）
- **`Location` の履歴はブラウザの戻るボタンとつながっていない。** `history.pushState` は使っておらず、
  画面内の「もどる」だけが対象。URL も変わらない。ルーティングの要否は未確定
- **AI 評価の閾値が仮の値。** FR-AI-EVAL-007 は「ある一定の閾値」としか書いていない。
  `lib/mockAiEvaluate.ts` の `EVALUATE_PASS_MAX_MONTHS = 36`（3歳以下を通す）は仮置きで、
  backend と揃えるべき数値
- **NFR-001 と NFR-003 が食い違っている。** NFR-001 は「AI が使えなくても投稿・あやすを継続できる」、
  改訂後の NFR-003 は「AI 評価が使えない場合は投稿を妨げる」。
  実装は新しい NFR-003 に従っている
- **右サイドの記事は中身が無い。** リンクが付く想定だが、これは本文の URL 禁止
  （FR-MOD-021 / OUT-008）とは別の話という理解で進めている
- **本文の文字数では絵1つ＝1文字として数えている。** 正式な数え方は未確定（design_doc §7.1 の注記）
- **スタンプの「気持ち」（`emotion`）は `GET /api/stamps` の応答に無い項目。**
  一覧を気持ち別に分けるために、モックデータ（`public/data/stamps.json`）が持っている。
  キーは `frontend/images/stamps/` のフォルダ名と揃えた。実 API では backend 側の追加が要る。**人間の確認が必要**
- **せってい画面は画面一覧（S1〜S8）に無い。** 中身はテーマの切り替え（DESIGN.md §8.3 が実装契約まで
  決めているもの）だけにして、おしらせ・なまえの変更は押せない「これから」の行にしてある。
  テーマ見本は `tokens/theme.css` の各ブロックを入れ子の `[data-theme]` にも効かせて描いている
- **あやす本文の文字数上限は設けていない。** 仕様に無いので勝手に決めていない
- **文字数の数え方**はコードポイント単位（`data/constants.ts`）。正式な数え方は未確定
  （design_doc §7.1 の注記）
- **`rewrite_required` で書き換え候補を返していない。** design_doc §7.1 の例では
  `transformedText` に候補を返すとあるが、DESIGN.md §4 は「使える形で出さない」。
  仕様書（FR-AI-TRANS-004）はどちらも満たせるため、厳しい側の DESIGN.md に合わせた。**人間の確認が必要**
- **`reasonCodes` を返していない。** design_doc §7.1 の応答例にはあるが、
  FR-AI-TRANS-009 と衝突するため上位文書に従った。**backend の I/F を決めるときに要確認**
- **自分のバブルへの「あやす」を赤ちゃんに限定している**（`lib/soothePersonaRule.ts`）。
  仕様書の制約ではなく、DESIGN.md §0.1-1「同じ画面に自分の両ペルソナを並べない」から導いた
  UI 側の判断。**人間の確認が必要**（リアクション側は 2026-08-24 に決定済みだが、
  あやす側のペルソナ選択は未決定）
- **自分のあやすへのリアクション禁止は、バブルの決定を同じ理由で広げたもの。**
  決定は「自身のバブルに自分でリアクションすることは不可」という文言だったので、
  あやすへの適用は推測を含む。分けるべきなら教えてください
- **意味色（`--success` / `--warning` / `--error`）と `--mother-fill` はどの画面でも未使用。**
  トークン契約（DESIGN.md §2.1）にある役割なので定義だけ置いてある。
  園児UI の意味色は DESIGN.md に値が無いため、ノーマルの値を継承している
- **園児UI の鮮やか色（`vivid-*`）はトークン化していない。** ノーマル・ダーク側に対応する値が
  DESIGN.md に無く、「メディアクエリや `[data-theme]` の中だけで定義される色を作らない」規約
  （§8.3）と両立しないため。園児UI の性格は 2px 枠・ずらし影・傾き・角丸・黄色い地で出している
- **アイコンは仮のもの**（DESIGN.md §11 でアイコンセット未定）。`components/icons.tsx` を差し替える前提
- **`Zen Maru Gothic` は Google Fonts の `<link>` で読んでいる。** セルフホストにするかは未確定（§11）
- **エラー表示の共通形式は未確定**（backend のエラー形式そのものが未確定 / Issue #8）。
  `data/api.ts` は粗い区分（`moderation` / `too_long` / `persona_not_allowed`）だけを返す
- **`--hairline` の枠線と `surface`／`canvas` の差が WCAG 1.4.11 の 3:1 を割っている。**
  DESIGN.md §2 に規約が無い箇所で、パレットの変更は人間の判断。Issue で相談中。
  ダークを紺にしたあとも同じ（`hairline` vs `canvas` 1.34:1）。**刷新で悪化はしていない**
- **お母さんの緑は、輝度と面積だけ手当てした。** 臙脂との色相差 152°（ほぼ補色）は残っている。
  色相を動かす案（藤 `#AC8DB3`）は「お母さん＝緑」というコンセプトの変更になるので、
  人間の決定待ち（`docs/color_and_ui_findings.md` §7-2）
- **ノーマルの `mother` と `success` がまだ同じ hex**（`#39704E`）。
  ダークは `mother` を下げた副作用で別の色になった。`success` は未使用なので表には出ていない
- **ダークの文字は暖色の生成りのまま。** 紺の上で黄ばんで見えるかは実機判断（未決）
- フロントの API ベース URL 用の環境変数は、接続しないので置いていない
- **バブルカードの視線順を「本文 → 書いた人」に変えたのは実装側の判断**（上の食い違い表）。
  DESIGN.md §4 Cards は逆の順序を指定している。**人間の確認が必要**
- **右サイドの名前が、登録直後に新しいニックネームへ変わらない。**
  `me` は起動時に `fetchMe()` で一度だけ読んでおり、モックの `createAccount` は
  固定の `ME` を返す。刷新の前からある挙動で、今回は触っていない
- **イラストは 3 種 × 3 テーマの 9 ファイルすべてを使っている。**
  ただしテーマごとに出るのは 1 ファイルなので、1 回の閲覧で目に入るのは最大 3 種

## 独立監査（DESIGN.md §9.3）で直したもの

作った本人の自己採点は当てにならないので、別の文脈で §9.3 の監査プロンプトを回した。
指摘のうち、実装側で直せるものは直した。

| 指摘 | 直す前 | 直した内容 | 理由 |
|---|---|---|---|
| コメントと実装の食い違い | `ScreenHeader.css` の先頭コメントが「上 padding を 1.07 倍にする」と書いているのに、実装は上下とも `--space-sm` で同値だった | `--head-padding-top: 14px` / `--head-padding-bottom: 13px` を足して 1.077 倍にした | コメントが主張する効果を実装が出していなかった。12px 基準では 1.05〜1.08 倍の中に整数が無い（12.6〜12.96px）ので、整数で収まる 14 / 13 を採った（DESIGN.md §5.3-1、±2px 以内） |
| トークンを経由しない生の px | `TimelineScreen.css` の帯の罫線が `border-left: 2px` の直書き | `calc(var(--border-width) * 2)` にした | 一点物の装飾でも線の太さはテーマに従うべき。園児UI では 4px になる |
| `rewrite_required` と `block` が読み分けられない | 見出しが両方「このままでは投稿できません」で、アイコンもどちらも盾だった | `block` の見出しを「匿名性を守るため、投稿できません」にし、`rewrite_required` のアイコンをペンにした | 疲れているときに本文の一行だけで読み分けさせない。見出しに理由が入るぶん FR-MOD-033 にも寄る |
| 下部ナビの現在地が色と太さだけ | `.is-current` が色＋`font-weight: 700` のみ | 現在地に印（モバイルは上の横棒、デスクトップは左の縦棒）を足した | 色だけで情報を伝えない（DESIGN.md §2.5）。他の要素は二重化できていたのにここだけ弱かった |
| トークンに無い生の ms | `Toast.tsx` の `VISIBLE_MS = 2600` に説明が無かった | アニメーションの長さではなく表示の保持時間だという理由をコメントにした | motion.css のトークンは遷移時間のスケールで、保持時間はその対象外 |

直していない指摘：

- **`hairline` の境界線が 3:1 を割っている**（上記）。パレットは見た目の正本を持つ人間の判断なので、
  Issue に出して待っている
- **S3 の「バブルはいつでも赤ちゃん」の注記と、その下の `disabled` ボタンが同じことを二度言っている**。
  どちらも DESIGN.md が指定している（§4 Note Box の用例と §0.3）ので、そのまま残した
- **仕様書に無い独自判断が人間承認前に有効になっている**（自分のバブルへのあやすの制限）。
  指摘は正しい。Issue で判断を待っている。実装は `lib/soothePersonaRule.ts` の1か所に集約してあり、
  方針が変われば1か所の変更で済む

## モックであることの限界

- **フロント側のモデレーションと文字数チェックは、保証にはならない。**
  本番では保存時にサーバ側で必ず再検査する（FR-MOD-003/004、NFR-004）。
  `data/api.ts` の検査は「境界で弾く形」を示すためのもの
- 同じく、リアクションの組み合わせ（FR-REACT-007）とあやすの返信ペルソナ（FR-COMMENT-006）も、
  サーバ側で弾くのが本体。フロントは「そもそも出さない」を担保する側
- 再読み込みするとダミーデータは初期状態に戻る（保存先を持たない）

## DESIGN.md との食い違い（改訂案を出す前提で先に実装している）

人間の決定（2026-08-25）で PC 3カラムに切り替えたため、次の3点が正本と食い違っている。

| DESIGN.md | 実装 |
|---|---|
| §5.2「カラムを増やさない（2列にするとバブルを流し読みする対象に変えてしまう）」 | 3カラム。ただし中央のフィードは 600px のまま広げていない |
| §8.1「モバイルファースト・既定は 375px・主要アクションは画面下 1/3」 | PC 既定。バブるボタンは中央の右下 |
| §0.2「1種のリアクションは左寄せ」 | 右寄せ（押しやすい側に寄せる） |
| §4 Cards「視線は ニックネーム → 本文 → リアクション の順」 | **本文 → 書いた人 → リアクション**（UI刷新 2026-08-26）。吹き出しの中に本文、しっぽの先に書いた人を置いたため。§0（Non-Negotiables）の項目ではないので実装側で判断したが、**見た目の正本を持つ人間の確認が要る**。DOM とキーボードの移動順も同じ順序（入れ子のボタンは作っていない） |
| §6「影はモーダルとトーストのみ」 | 左右の「島」（LeftRail / RightRail）と Intro のカードにも `--elevation-offset` / `--elevation-modal` を当てている。縦の仕切り線を外したぶん、列の切れ目を面で読ませるために要った。ノーマル・ダークでは `--elevation-offset` が `none` なので、実際に増えたのは Intro のカードの影だけ |

§7.3「MVP で入れる動きは5つだけ」については、パネルの出入りを `modal` 型の使い回しとして扱い、
中央が左へ詰まる動きも同じ duration / easing にして、ひとつの運動として読ませている。
2026-08-26 に足した動き（送信ボタンの pop・完了トーストの波紋）も、
新しい型を作らず `like-button` / `ring-out` を使い回している。

### DESIGN.md 側を書き換えたところ（2026-08-26）

人間の決定（`docs/color_and_ui_findings.md` §0）に合わせて、**正本のほうを直した**。
申し送りが「値だけ差し替えると、次に読む人が矛盾を踏む」と警告していた箇所。

| DESIGN.md | 直した内容 |
|---|---|
| frontmatter `themes.dark` | 地を紺へ。`mother` を `#4AAA6E` へ |
| frontmatter `themes.kid` | `on-accent: "#FFFDF9"` を追加（実装にはあったが文書に無かった） |
| §2.1 トークン表 | `mother-soft` の用途を「お母さんあやすの地」→「役割バッジの地」に |
| §2.2 | 配色を差し替えるときの条件に **6.「`mother` は `accent-line` より目立たない」** を追加 |
| §2.3 | 「暖色寄りにして生成りと同じ体温にする」を**引用として残したまま**、「ダークは夜空側に振る」へ差し替え。値と実測も更新 |
| §2.6（新設） | 「主役は赤ちゃん側」。輝度・色相差・面積の3つに分けた手当てと、塗りをやめられる根拠 |
| §4（新設） | Hover の決めごと（`ink` 8% を重ねる／混ぜる先の選び方／浮かせない） |
| §4 Buttons | 主操作の Press を `scale(0.97)` に。「押す前の誘い」は1つだけ |
| §4（新設） | あやすカードの規定（役割色で塗らない） |
| §7.5 | reduced motion を実装に合わせて更新（`* { animation-duration: 1ms }` をやめる） |
