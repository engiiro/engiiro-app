/*
 * モデレーションの `block` 表示を確認するためだけのサンプル入力。
 *
 * ここに置いたものはフィードにもバブルにも流していない（BUBBLE_SEEDS / SOOTHE_SEEDS から参照しない）。
 * S3 のモック操作欄から本文入力欄へ差し込む用途だけに使う。
 *
 * 意図的に次に触れている：
 *   - 外部サービスでの連絡への誘導（FR-MOD-017）
 *   - 現実世界での待ち合わせ（FR-MOD-018）
 *   - 連絡先を渡そうとする内容（FR-MOD-016）
 * 実在の連絡先・実在の場所は書いていない。
 */
export const BLOCK_DEMO_INPUT =
  "もう限界。だれか話きいてほしい。今夜9時に駅の改札で待ってるから来て。連絡先おくるので個別に連絡ください。";

/**
 * `rewrite_required`（攻撃的・否定的な表現＝マサカリ表現、FR-MOD-023）を確認するためのサンプル。
 * 個人は特定できないが、そのままでは使わせない側の例。
 */
export const REWRITE_DEMO_INPUT = "こんな設計にしたやつ、正直ばかなんじゃないの。無能すぎて話にならない。";
