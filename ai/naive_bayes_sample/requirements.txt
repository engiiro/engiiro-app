# Web API のフレームワーク。
fastapi>=0.115,<1.0

# FastAPI アプリを実際に動かす ASGI サーバー。
# [standard] を付けると、--reload（コード変更時の自動再起動）などに必要な
# 追加パッケージも一緒に入る。
uvicorn[standard]>=0.30,<1.0

# 日本語の形態素解析（文章を単語に分割する処理）のためのライブラリ。
# [unidic-lite] を付けると、辞書データも一緒にインストールされるため、
# 別途 MeCab 本体をシステムにインストールする必要が無い。
fugashi[unidic-lite]>=1.3,<2.0
