fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# 文章変換（ai/transform_api.py）
# Issue #15 の決定により、ローカルモデルではなく
# Google AI Studio の API を呼ぶ方式にした。
google-genai==2.19.0

# モデレーションの形態素解析（ai/moderation_rules.py）
# 品詞を見ないと「バカ」と「ばかり」、「クズ」と「くずれる」を
# 区別できず、日常語を弾いてしまう。
# 無くても動くが、ひらがな表記のNG語を見逃す。
janome==0.5.0

# 本格的なモデルを組み込む際に使う想定（docs/design_doc.md 5章参照）
torch==2.4.1
scikit-learn==1.5.2
