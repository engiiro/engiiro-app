---
version: 2
name: engiiro-design
description: >
  「えんじいろ」＝エンジニアのための本音吐露SNS の見た目の正本。3つのテーマ（ノーマル／ダーク／園児UI）を
  1つのトークン契約の上に載せる構成をとる。どのテーマでも骨格（余白・書体階層・動き・コンポーネントの
  形）は共通で、差し替わるのは色だけ。ノーマルの配色は検討中のため暫定値。ダークは臙脂をそのまま置くと
  沈むため「線・文字の臙脂」と「塗りの臙脂」を別トークンに割る。園児UIは鮮やかさを優先し、面ごとに
  載せられる文字色を実測で決める。

themes:
  # ── ノーマル（検討中。値は暫定） ─────────────────────────────
  normal:
    status: 検討中（Issue で確定させる）
    canvas: "#FFF8F1"
    surface: "#FFFDF9"
    card: "#F1E7DE"
    hairline: "#E8DED6"
    ink: "#312624"
    body: "#4A3C37"
    muted: "#6E615C"
    accent-line: "#9B3342"
    accent-fill: "#9B3342"
    accent-fill-large: "#8E2F3B"
    accent-press: "#7D2633"
    accent-soft: "#F2E4E6"
    on-accent: "#FFFDF9"
    baby: "#9B3342"
    baby-soft: "#F2E4E6"
    mother: "#39704E"
    mother-fill: "#4F8F69"
    mother-soft: "#E3F1E9"
    success: "#39704E"
    warning: "#8C5A0C"
    error: "#8C2F1E"
  # ── ダーク（実測済み） ───────────────────────────────────────
  dark:
    status: 確定候補（実測でコントラスト検証済み。地は 2026-08-26 の人間の決定で紺へ）
    canvas: "#11151C"
    surface: "#161B24"
    card: "#1C222E"
    hairline: "#262E3E"
    ink: "#F4EDE4"
    body: "#DCD1C6"
    muted: "#A8998E"
    accent-line: "#E28E9C"
    accent-fill: "#B54254"
    accent-fill-large: "#B54254"
    accent-press: "#C85C6D"
    accent-soft: "#3A2226"
    on-accent: "#FFFDF9"
    baby: "#E28E9C"
    baby-soft: "#3A2226"
    # mother は accent-line より目立たない値にする（§2.2 条件6）。#7FC79A は 9.18:1 で主役を追い越していた
    mother: "#4AAA6E"
    mother-fill: "#6FB88C"
    mother-soft: "#1F2E25"
    success: "#7FC79A"
    warning: "#E0B25C"
    error: "#E88A78"
  # ── 園児UI（実測済み・鮮やかさ優先） ─────────────────────────
  kid:
    status: 確定候補（面ごとの文字色を実測で確定）
    canvas: "#FFF9E3"
    surface: "#FFFFFF"
    card: "#FFF3C4"
    hairline: "#E8C84A"
    ink: "#2B211C"
    body: "#5A4A40"
    muted: "#7A675C"
    accent-line: "#C22648"
    accent-fill: "#C22648"
    accent-fill-large: "#C22648"
    accent-press: "#A31E3C"
    accent-soft: "#FFE0E6"
    on-accent: "#FFFDF9"
    baby: "#C22648"
    baby-soft: "#FFE0E6"
    mother: "#2E7A50"
    mother-fill: "#7FB069"
    mother-soft: "#E4F3DC"
    vivid-yellow: "#F4C51F"
    vivid-orange: "#EF7A22"
    vivid-green: "#7FB069"
    vivid-pink: "#F2A0B5"
    vivid-blue-deep: "#1F6B87"
    vivid-purple-deep: "#6650A0"
    vivid-blue-pale: "#A8D8E8"
    vivid-purple-pale: "#CFC4EC"
    on-vivid-light: "#2B211C"
    on-vivid-dark: "#FFFDF9"

typography:
  display:
    fontFamily: '"Zen Maru Gothic", "Hiragino Maru Gothic ProN", "Hiragino Sans", Meiryo, sans-serif'
    fontSize: 24px
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: 0.02em
    palt: true
  heading: { fontSize: 18px, fontWeight: 700, lineHeight: 1.5, letterSpacing: 0.02em, palt: true }
  card-title: { fontSize: 15px, fontWeight: 700, lineHeight: 1.5, letterSpacing: 0.02em, palt: true }
  bubble-body:
    fontFamily: '-apple-system, system-ui, "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Yu Gothic UI", Meiryo, sans-serif'
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.9
    letterSpacing: normal
    palt: false
  body: { fontSize: 15px, fontWeight: 400, lineHeight: 1.75, letterSpacing: normal, palt: false }
  label: { fontSize: 12px, fontWeight: 700, lineHeight: 1.7, letterSpacing: 0.04em }
  caption: { fontSize: 12px, fontWeight: 400, lineHeight: 1.6, numeric: tabular-nums }
  button: { fontSize: 15px, fontWeight: 700, lineHeight: 1.0, letterSpacing: 0.06em }
  metric: { fontSize: 19px, fontWeight: 700, lineHeight: 1.3, numeric: tabular-nums }
  counter: { fontSize: 12px, fontWeight: 700, numeric: tabular-nums }
  mono: { fontFamily: "SFMono-Regular, Consolas, Menlo, monospace", fontSize: 14px, lineHeight: 1.5 }

spacing: { xxs: 4px, xs: 8px, sm: 12px, md: 16px, lg: 24px, xl: 32px, xxl: 48px }
rounded: { xs: 4px, sm: 6px, md: 10px, lg: 16px, pill: 999px }

motion:
  duration-stagger: 40ms
  duration-micro: 80ms
  duration-quick: 150ms
  duration-fast: 250ms
  duration-medium: 350ms
  duration-slow: 400ms
  duration-very-slow: 500ms
  ease-smooth-out: cubic-bezier(0.22, 1, 0.36, 1)
  ease-in-out: ease-in-out
  ease-bounce: cubic-bezier(0.34, 1.36, 0.64, 1)
  ease-linear: linear
  distance-micro: 4px
  distance-base: 8px
  distance-medium: 12px
  scale-modal: 0.96
  blur-small: 2px
  blur-medium: 3px

layout:
  feed-max-width: 600px
  gutter: 16px
  touch-target-min: 44px
  breakpoint-tablet: 600px
  breakpoint-desktop: 960px
  bubble-max-length: 150
---

# DESIGN.md — えんじいろ

## このファイルの位置

正本の階層は次のとおり。**上に書かれたものが常に優先**する。

| 順 | 文書 | 決めていること |
|---|---|---|
| 1 | [`AGENTS.md`](AGENTS.md) | AI エージェントの作業規則（担当境界・Git・Issue 上の会話） |
| 2 | [`docs/specification.md`](docs/specification.md) | **何を満たすか。**利用者から見える振る舞いと受入条件の正本 |
| 3 | [`docs/design_doc.md`](docs/design_doc.md) | 全体設計書。構成・責務・設計方針 |
| 4 | **このファイル** | 色・書体・余白・動き・コンポーネントの形、および AI への発注の型 |

このファイルが上位文書と矛盾したら**上位が正**。こちらを直して Issue で共有する。
仕様の疑問を推測で埋めない（`AGENTS.md`）。

用語は `docs/specification.md` §3.1 に従う。**投稿＝バブル、コメント＝あやす。**
以下、UI 上の表記もこの語を使う。

---

## 0. Non-Negotiables（仕様由来。色より先に守る）

すべて `docs/specification.md` の受入条件そのもの。**見た目の判断でここを曲げない。**
とくに §15.1「重点確認項目」の6点は、破れるとサービスの前提が崩れる。

### 0.1 ペルソナ非連結（FR-PERSONA-003/004、FR-PRIV-004）

1. **同じ画面に自分の両ペルソナを並べない。** S8（本人専用プロフィール）だけが例外。
2. **ペルソナ色は「役割」の色。個人の色にしない。** 赤ちゃん系／お母さん系は全利用者共通。
   利用者ごとに色を割り当てると、それ自体が同一人物の手がかりになる。
3. **アバターを自動生成しない。** `accountId` や ID を種にした identicon・グラデーション・図形生成は禁止。
   両ペルソナで同じ絵柄が出て非連結が崩れる。頭文字と役割色だけで足りる。
4. **公開プロフィール（S6）に、もう一方のペルソナへの導線を置かない。** リンクも、履歴も、示唆も。
5. **時刻は相対表示（「3分前」）。** 秒精度の絶対時刻は、2ペルソナの投稿時刻を突き合わせる材料になる。
   `title` 属性やツールチップにも絶対時刻を入れない。

### 0.2 リアクションは「対象によって種類が変わる」（FR-REACT-001〜009）

**これが今いちばん間違いやすい UI。** リアクション行は固定の3ボタンではない。

| 対象 | 出すボタン |
|---|---|
| バブル | `おぎゃー` `よしよし` `わかるわぁ` の**3種だけ** |
| 赤ちゃんとしてのあやす | `おぎゃー` `よしよし` `わかるわぁ` の**3種だけ** |
| お母さんとしてのあやす | `ばぶー` の**1種だけ** |

- **`わかるわぁ` と `哺乳瓶` は同一のリアクション。** 2つのボタンを作ってはいけない（FR-REACT-009）。
  UI 上の解決：**ラベルは「わかるわぁ」、アイコンは哺乳瓶。ボタンは1つ。**
- お母さんあやすに `おぎゃー`／`よしよし`／`わかるわぁ` を出さない（FR-REACT-006）。
- バブル・赤ちゃんあやすに `ばぶー` を出さない（FR-REACT-004）。
- **画面から隠すだけでは不可**（FR-REACT-007）。サーバ側でも弾く。フロントは「弾かれる前提で、
  そもそも出さない」を担保する側。
- 否定的・攻撃的なリアクションを作らない（FR-REACT-008）。低評価も通報も置かない。

→ リアクション行は「対象の種類」を入力にとり、ボタン数が 3 か 1 に変わるコンポーネントとして作る。
1種のときは横幅を引き伸ばさず左寄せにする（3種の行と同じリズムを保つ）。

### 0.3 あやすの返信ペルソナ制約（FR-COMMENT-005〜007）

**お母さんとしてのあやすに返信するときは、ペルソナ選択に「赤ちゃん」しか出さない。**
「お母さん」を `disabled` で見せるのではなく、**選択肢そのものを出さない**（FR-COMMENT-005 の
受入条件が「お母さんが選択肢に現れず」）。
一方、S3（バブル作成）では逆で、お母さんを `disabled` にして理由をラベルに書く
（投稿は常に赤ちゃんという仕様を、隠して誤解させないため）。**この2つを混同しない。**

### 0.4 モデレーション拒否時の見せ方（FR-MOD-030〜034、FR-PRIV-006）

- 拒否理由は**匿名性の保護として説明する**（FR-MOD-033）。「NG表現です」だけの表示は不可。
  文言の基準（Issue #11 の人間決定より）：
  > えんじいろでは、あなたと他の利用者の匿名性を守るため、個人が特定できる情報・外部連絡先・
  > 実際に会うための内容は投稿できません。
- **判定の内部情報を出さない**（FR-MOD-034、FR-PRIV-006）。`reasonCodes`、辞書の中身、
  マッチした文字列、内部エラーを画面に出さない。
- **伏せ字にして自動投稿しない**（FR-MOD-031）。書き換え案を出す場合も、利用者の確認なしに保存しない。
- 投稿に関わる画面に、特定情報を書かないよう促すガイドラインを置く（FR-PRIV-001）。

### 0.5 存在させない UI（16章 非スコープ）

作らないものは、`0` と表示するのではなく**要素そのものを置かない**。

| 置かないもの | 根拠 |
|---|---|
| フォロワー数・フォロワー一覧 | FR-FOLLOW-004/005、OUT-004。**本人にも見せない** |
| 通報ボタン | OUT-002（MVP 非スコープ） |
| DM の入口 | OUT-001。ニックネームのタップ先は常に S6 だけ |
| 本文中のリンク（URL） | FR-MOD-021、OUT-008。**本文の URL を自動リンク化しない**。そもそも保存されない |
| 否定的リアクション | FR-REACT-008、OUT-003 |
| ストレス傾向の可視化 | OUT-006 |

### 0.6 AI の結果を勝手に確定しない（FR-AI-TRANS-006/007、NFR-001〜003）

- 変換結果を**自動でバブル／あやすにしない。** 必ず「確認して、必要なら直して、自分で保存する」を通す。
- AI が止まっていても**バブルの投稿とあやすは続けられる**（NFR-001）。
  評価が失敗しても投稿ボタンを無効化しない（NFR-003）。
- AI が使えないことを利用者に伝える（NFR-002）。処理中であることを示す（NFR-006）。

---

## 1. Visual Theme & Atmosphere

### 1.1 3つのテーマを持つ

| テーマ | 狙い | 状態 |
|---|---|---|
| **ノーマル** | 深夜に片手で、疲れた状態で長く読み書きしても疲れない編集面 | **配色は検討中。**骨格は確定 |
| **ダーク** | 夜そのものの明るさ。ノーマルより静かで、光を目に入れない | 実測済み。**臙脂の扱いを変える必要がある**（§2.3） |
| **園児UI** | カラフルで鮮やか。可読性より「園児の工作」らしさを優先する | 実測済み。**面ごとに載せられる文字色が決まる**（§2.4） |

### 1.2 テーマ間で共通なもの（＝骨格）

**差し替わるのは色だけ。**次はテーマをまたいで同じにする。

- 余白スケール（§5）
- 書体階層とサイズ・行間（§3）
- コンポーネントの構造と配置（§4）
- 動きのトークンと規則（§7）
- レイアウトとブレークポイント（§8）

理由：3テーマ分の構造を別々に持つと、ハッカソン期間で3倍の修正が発生する。
色だけを CSS 変数で差し替える形にしておけば、テーマ追加は「変数セットを1つ足す」で済む。

### 1.3 テーマごとの「装飾の量」

骨格は同じでも、**装飾の強さはテーマの性格として変えてよい**。

| | 枠線 | 影 | 角丸 | 傾き・不定形 |
|---|---|---|---|---|
| ノーマル | 1px 実線 | なし（モーダル／トーストのみ） | 4 / 10 / 16 | なし |
| ダーク | 1px 実線（面色の差でも代替可） | なし | ノーマルと同じ | なし |
| 園児UI | **2px 実線・破線** | **6px 6px 0 のずらし影** | ノーマル + 不定形の角丸 | **-2.5deg 程度** |

園児UI の装飾は「あってよい」ではなく**このテーマの本体**。ここを削ると園児UI である意味がなくなる。

---

## 2. Color Palette & Roles

### 2.1 トークン契約（テーマに依存しない役割名）

**コンポーネントは必ずこの役割名を参照する。** 生の hex をコンポーネント側に書かない。
テーマは「この表を埋めた変数セット」として定義する。

| 役割 | 用途 |
|---|---|
| `canvas` | ページの地 |
| `surface` | カード・入力欄・ヘッダの面 |
| `card` | canvas より一段沈めた面（セグメント地・タグ地・メーター地） |
| `hairline` | 1px の枠線 |
| `ink` | 見出し・ニックネーム |
| `body` | バブル本文・あやす本文 |
| `muted` | 時刻・タグ・補足 |
| `accent-line` | **線・文字・アイコンの臙脂。**リンク、活性状態、枠線 |
| `accent-fill` | **塗りの臙脂。**主ボタンの背景 |
| `accent-fill-large` | 面積の大きい塗り（48px 超の全幅ボタン、完了帯） |
| `accent-press` | 押下・ホバー |
| `accent-soft` | 活性リアクションの地、注記ボックスの地 |
| `on-accent` | 臙脂の塗りの上に載る文字 |
| `baby` / `baby-soft` | 赤ちゃんペルソナ（役割色。§0.1-2） |
| `mother` | お母さんペルソナ **文字・枠線用** |
| `mother-fill` | お母さんペルソナ **塗り専用**（文字に使わない） |
| `mother-soft` | お母さんの役割バッジの地（**あやすカード全面には塗らない。**§2.6） |
| `success` / `warning` / `error` | 意味色 |

**`accent-line` と `accent-fill` を分けているのがこの契約の要。**
ノーマルでは同じ値でよいが、ダークでは必ず別の値になる（§2.3）。
ライトで1色に見えるものが、暗い地では2色に割れる。

### 2.2 ノーマル（**配色は検討中**）

下の値は `frontend/color-check` の検討で使っていた「避難所 01」相当の**暫定値**。
**確定していない。** ここを決めるのが今の宿題。

```
canvas #FFF8F1   surface #FFFDF9   card #F1E7DE   hairline #E8DED6
ink #312624   body #4A3C37   muted #6E615C
accent-line/fill #9B3342   accent-fill-large #8E2F3B   accent-press #7D2633
accent-soft #F2E4E6   on-accent #FFFDF9
baby #9B3342 / #F2E4E6      mother #39704E（文字）/ #4F8F69（塗り）/ #E3F1E9
success #39704E   warning #8C5A0C   error #8C2F1E
```

実測コントラスト（canvas `#FFF8F1` 上）：

| 前景 | 比 | 判定 |
|---|---|---|
| `#312624` ink | 13.9:1 | AAA |
| `#4A3C37` body | 10.0:1 | AAA |
| `#6E615C` muted | 5.7:1 | AA |
| `#9B3342` accent | 6.8:1 | AA |
| `#8E2F3B` accent-fill-large | 7.6:1 | AAA |
| `#39704E` mother | 5.5:1 | AA |
| `#4F8F69` mother-fill | 3.65:1 | **文字は不可**（塗り専用） |
| `#6E615C` on `#F1E7DE`（タグ） | 4.9:1 | AA |
| `#39704E` on `#E3F1E9`（母バッジ） | 5.0:1 | AA |
| `#FFFDF9` on `#9B3342` | 7.0:1 | AAA |

**配色を差し替えるときの条件**（これを満たさない案は採らない）：

1. `body` が `canvas` 上と `surface` 上の両方で 4.5:1 以上
2. `muted` が `canvas` 上と **`card` 上の両方**で 4.5:1 以上
   → タグは沈めた面に載る。canvas 基準だけで色を決めると足りなくなる
3. `on-accent` が `accent-fill` 上で 4.5:1 以上
4. `accent-line` が `canvas` 上で 4.5:1 以上
5. 面積の大きい塗りには、小面積用より**わずかに暗い**値を用意する（§5.3-2 面積効果）
6. **`mother` は `canvas` 上で `accent-line` より低いコントラストであること**
   → 人間の決定 2026-08-26：「このSNSは赤ちゃん側が主役」。
   逆転すると、あやす側に目が行く画面になる。実測（ノーマル +1.23 ／ 園児UI +0.48 ／
   ダーク +1.15。いずれも赤ちゃんが前）

### 2.3 ダーク（**臙脂をそのまま持ち込むと沈む**）

#### 何が起きているか（実測）

```
#9B3342（臙脂）on #1A1513（暗い地）  = 2.54:1
```

WCAG の**UI 部品の下限 3:1 すら割っている。** 文字にも、枠線にも、アイコンにも使えない。
「映えない」のは主観ではなく、輝度差がほぼ無いという計測上の事実。
臙脂は明度が低く彩度が中程度なので、暗い地に置くと地に溶ける。

#### 解法：臙脂を「明るい線の色」と「塗りの色」に割る

| 役割 | 値 | 実測 |
|---|---|---|
| `accent-line`（文字・枠線・アイコン・活性状態） | `#E28E9C` | canvas 上 **7.4:1** ／ card 上 **6.3:1** |
| `accent-fill`（主ボタンの背景） | `#B54254` | canvas との差 **3.34:1**（部品として輪郭が見える） |
| `on-accent`（そのボタンの文字） | `#FFFDF9` | fill 上 **5.3:1** |
| `accent-press` | `#C85C6D` | — |
| `accent-soft`（活性リアクションの地） | `#3A2226` | — |

- `accent-line` `#E28E9C` は**臙脂と同じ色相のまま明度だけを上げた薄紅**。
  別のブランド色に逃げていないので「えんじいろ」であることは保てる。
- `accent-fill` を `#9B3342` のままにしないのは、**ボタンの輪郭が地から浮かないから**。
  `#B54254` は 3:1 を超える最小限の持ち上げ。

#### 地は紺（人間の決定 2026-08-26）

**ダークの地は暖色ではなく紺にする。**「夜空っぽく、おやすみモード的な雰囲気」にするため。

> **改訂前の理由づけ**（2026-08-26 に差し替え）
> 「純黒を地に使わない。`#1A1513` の暖色寄りにして、**ノーマルの生成りと同じ体温にする**」
>
> 体温を揃えるという狙いは、決定 1 で「ダークは夜空側に振る」に置き換わった。
> ノーマル（生成り）とダーク（紺）は体温が揃わないが、**それは意図した差**である。
> 揃えているのは体温ではなく、明度の段と、文字・臙脂の値のほう。

```
canvas #11151C   surface #161B24   card #1C222E   hairline #262E3E
ink #F4EDE4（15.8:1）   body #DCD1C6（12.2:1）   muted #A8998E（6.6:1 / card上 5.8:1）
mother #4AAA6E（6.3:1）   mother-fill #6FB88C   mother-soft #1F2E25
success #7FC79A   warning #E0B25C   error #E88A78
```

- **明度の段は暖色ダークのまま**（8.8 / 11.4 / 14.5 / 19.6%）。**色相と彩度だけを替えている。**
  暗い地では影が効かず、階層は面色差だけで作っているので、段の間隔を動かすと境目が消える。
- **文字と臙脂の値は変えていない。** 紺の上でも全条件を満たす（`accent-line` 7.48:1、
  `accent-fill` の輪郭 3.37:1、`on-accent` 5.34:1）。
- **純白 `#FFFFFF` を本文に使わない。** 暗い地で滲む（ハレーション）。`#F4EDE4` にする。
  文字は暖色の生成りのまま残してある。紺の上で黄ばんで見えるかは実機判断（未決）。
- **純黒 `#000000` を地に使わない。** `#11151C` の紺にする。
- 意味色もダーク用に明度を上げる。`#8C2F1E`（ノーマルの error）は暗い地で沈む。
- 影は効かない。ダークの階層は**面色の差**（canvas → surface → card）で作る。
- **`mother` を `#7FC79A` から `#4AAA6E` に下げた**（§2.2 条件6）。
  副次的に `success`（`#7FC79A` のまま）と別の色になった。
  以前は `mother` と `success` が同じ hex で、意味の違うものが同じ色だった。

### 2.4 園児UI（**面ごとに載せられる文字色が決まる**）

鮮やかさを優先するテーマ。ただし**鮮やかな面の上に何色の文字が載るかは選べない**。
中間の明るさの色は、暗い文字も白い文字もどちらも届かない。実測結果：

| 面（ベタ塗り） | 暗い文字 `#2B211C` | 白 `#FFFDF9` | 載せられる文字 |
|---|---|---|---|
| 黄 `#F4C51F` | 9.6:1 | 1.6:1 | **暗い文字のみ** |
| 桃 `#F2A0B5` | 7.8:1 | 2.0:1 | **暗い文字のみ** |
| 緑 `#7FB069` | 6.2:1 | 2.5:1 | **暗い文字のみ** |
| 橙 `#EF7A22` | 5.6:1 | 2.8:1 | **暗い文字のみ** |
| 臙脂 `#C22648` | 2.7:1 | 5.6:1 | **白のみ** |
| 青 `#2D8FB3` | 4.3:1 | 3.6:1 | **★どちらも不可** |
| 紫 `#8E7CC3` | 4.4:1 | 3.6:1 | **★どちらも不可** |

**規則：中間の明るさの色（青・紫）を「文字を載せる面」に使わない。**
線・小さい図形・アイコンには使ってよい。文字を載せたいときは、濃い版か淡い版を使う。

| 用途 | 値 | 実測 |
|---|---|---|
| 青（白文字を載せる面 / canvas 上の文字） | `#1F6B87` | 白 5.9:1 ／ canvas 上 5.7:1 |
| 紫（白文字を載せる面 / canvas 上の文字） | `#6650A0` | 白 6.4:1 ／ canvas 上 6.2:1 |
| 青（暗い文字を載せる淡い面） | `#A8D8E8` | 暗い文字 10.2:1 |
| 紫（暗い文字を載せる淡い面） | `#CFC4EC` | 暗い文字 9.5:1 |

**もうひとつの規則：鮮やかな色を canvas 上の文字色に使わない。**
`#EF7A22` を `#FFF9E3` の上に置くと 2.7:1、`#F4C51F` は 1.6:1。線と塗りの色であって、文字の色ではない。

```
canvas #FFF9E3   surface #FFFFFF   card #FFF3C4   hairline #E8C84A
ink #2B211C（14.9:1）   body #5A4A40（8.0:1）   muted #7A675C（5.1:1）
accent-line/fill #C22648（canvas上 5.4:1）   accent-press #A31E3C   accent-soft #FFE0E6
mother #2E7A50（文字）/ #7FB069（塗り）/ #E4F3DC
```

#### 「可読性が低いくらい鮮やか」をどこまで許すか

このテーマは鮮やかさが目的なので、装飾の彩度は上げてよい。ただし**1本だけ線を引いておく**：

> **バブル本文とあやす本文だけは 4.5:1 を守る。それ以外（見出し・タグ・装飾・空状態）は下げてよい。**

理由は美観ではなく、このサービスが**弱音を読み書きする場**だという点にある。
装飾が読みにくいのは「賑やか」だが、本音が読みにくいのは機能の欠落になる。
この1本を外す判断は人間の決定として Issue に残す（このファイルで勝手に決めない）。

### 2.5 全テーマ共通の規約

- **純黒 `#000000` を文字色に使わない**（園児UI の太枠など、装飾の線としてなら可）
- **色だけで情報を伝えない。** リアクションの活性、エラー、ペルソナの区別は、
  色＋文字（または形）で二重化する
- `error` と `accent` は色相が近い。**必ずアイコン＋文言を添える**
- テーマ切替は色だけ。**同じ要素がテーマによって違う場所に来ることがあってはならない**

### 2.6 主役は赤ちゃん側（人間の決定 2026-08-26）

**このSNSは赤ちゃん側が主役。** お母さんのあやすに目が奪われる状態を作らない。

目を引く強さは輝度差だけでは決まらない。**輝度 × 色相差 × 彩度 × 面積**で決まる。
お母さんの緑（`#7FC79A` 系）は臙脂と**色相差 152°**＝ほぼ補色なので、
明度を下げても補色の振動は残る。したがって手当ては2つに分ける。

| 原因 | 手当て | どこ |
|---|---|---|
| 輝度が主役より高い | `mother` を暗くする（§2.2 条件6） | `themes.dark.mother` |
| 塗り面積が広い | **あやすカードの地と枠を役割色で塗らない** | §4 あやすカード |
| 補色（色相差 152°） | **未決。**色相を動かすと「お母さん＝緑」の意味づけを捨てることになる | 人間の判断待ち |

**あやすカードを役割色で塗らない根拠は §2.5 にある。** §2.5 が求めているのは
「色＋文字（または形）」の二重化で、お母さんのあやすはすでに満たしている：

1. アバターの「母」
2. ロールバッジの文字「お母さん」（地は `mother-soft`、文字は `mother`）

**カード全面の塗りは3重目で、識別には要らない。**
もう一度 面で示したくなったときは、面積のいちばん小さいところから足す。カード全面には戻さない。

---

## 3. Typography Rules

### 3.1 和文フォント

- **丸ゴシック（display 専用）**：Zen Maru Gothic（Google Fonts, 700）／`Hiragino Maru Gothic ProN`
- **ゴシック（本文・UI）**：OS 標準（ヒラギノ角ゴ ProN → Yu Gothic UI → Meiryo）
- **明朝**：使わない

**なぜ display だけ Web フォントか。** 「えんじ＝園児」の語感を、装飾ではなく**書体の丸みで**背負わせる。
見出し・ボタン・数値に限れば読み込む文字数が少なく、`display=swap` のフォールバックも
`Hiragino Maru Gothic ProN` で丸ゴシックのまま着地する。本文を Web フォントにすると初回表示が
重くなり、深夜のモバイル回線という利用状況に反する。

### 3.2 欧文フォント

- **サンセリフ**：`-apple-system`, `system-ui`（和文より先に置き、英数字の品質を優先）
- **等幅**：`SFMono-Regular, Consolas, Menlo, monospace`（愚痴に混ざるコード片・エラーメッセージ用）

### 3.3 font-family 指定

```css
--font-display: "Zen Maru Gothic", "Hiragino Maru Gothic ProN",
  "Hiragino Sans", Meiryo, sans-serif;

--font-body: -apple-system, "system-ui", "Hiragino Sans",
  "Hiragino Kaku Gothic ProN", "Yu Gothic UI", Meiryo, sans-serif;

--font-mono: SFMono-Regular, Consolas, Menlo, monospace;
```

**フォールバックの考え方**

- display は Web フォントを先頭に置き、全 OS で同じ丸みを出す。落ちても丸ゴシックに着地する順序にする
- 本文はシステムフォント先頭。`-apple-system` → `system-ui` → 和文 → generic
- **和文フォントを1つだけ書かない。** 環境依存で崩れる
- generic family（`sans-serif`）を必ず最後に置く
  [出典: awesome-design-md-jp CONTRIBUTING の必須項目 / zenn DESIGN.md のシステムフォント構成]

### 3.4 文字サイズ・ウェイト階層

**3テーマで共通。** 色だけが変わる。

| Role | Font | Size | Weight | Line Height | Letter Spacing | palt | 用途 |
|---|---|---|---|---|---|---|---|
| Display | display | 24px | 700 | 1.4 | 0.02em | ○ | 画面タイトル |
| Heading | display | 18px | 700 | 1.5 | 0.02em | ○ | セクション見出し |
| Card Title | display | 15px | 700 | 1.5 | 0.02em | ○ | ニックネーム |
| **Bubble Body** | body | **16px** | 400 | **1.9** | normal | × | **バブル・あやす本文** |
| Body | body | 15px | 400 | 1.75 | normal | × | UI 本文・説明文 |
| Label | body | 12px | 700 | 1.7 | 0.04em | × | タグ、ロールバッジ |
| Caption | body | 12px | 400 | 1.6 | normal | × | 時刻・メタ（`tabular-nums`） |
| Button | display | 15px | 700 | 1.0 | 0.06em | ○ | ボタンラベル |
| Metric | display | 19px | 700 | 1.3 | 0 | × | 推定年齢（`tabular-nums`） |
| Counter | body | 12px | 700 | 1.4 | 0 | × | 文字数カウンタ（`tabular-nums`） |
| Code | mono | 14px | 400 | 1.5 | normal | × | コード片 |
| Input | body | **16px** | 400 | 1.8 | normal | × | 入力欄（16px 未満は iOS が拡大する） |

### 3.5 行間・字間

- **バブル本文の行間 `1.9`。** このサービスで一番重要な数値。
  [出典: note が記事本文で `18px / line-height 2.0`、zenn が `16px / 1.8`。
   えんじいろは 150 文字上限の短文なので、その中間の 1.9 を採る]
- 見出しの行間 `1.4`〜`1.5`
- **`letter-spacing` と `palt` は見出し専用。本文には入れない。**
  [出典: note DESIGN.md 実測 ─ h1/h2/h3 は `0.04em` + `palt`、本文 `p` は `normal`。
   本文の可読性は字送りではなく行間で確保している]
- 例外的に `0.04em` を許すのは 12px のラベルだけ。小さい字は少し空けたほうが読める
  [出典: apple-design ─ tracking はサイズ別。大きい文字は詰め、小さい文字は少し空ける]

### 3.6 禁則処理・改行ルール

```css
/* 本文（日本語 + 技術用語の英単語が混ざる） */
word-break: normal;
overflow-wrap: anywhere;
line-break: strict;

/* コード片のみ */
.code { word-break: break-all; }
```

`word-break: break-all` を本文に当てない。愚痴には `TypeScript` `deployment` のような
英単語が高頻度で混ざり、単語の途中で切れて読みにくくなる。日本語は `normal` で自然に折り返り、
長い識別子は `overflow-wrap: anywhere` が処理する。

**URL は本文に存在しない**（FR-MOD-021）。URL の折り返しを考える必要はなく、
**自動リンク化の処理も置かない**。

### 3.7 OpenType 機能

```css
.display { font-feature-settings: "palt" 1, "kern" 1; }  /* 見出し・ボタンのみ */
```

`palt` は見出しの字面を締めるのに効くが、本文に当てると行ごとに字送りが揺れて読みづらい。
[出典: zenn / qiita はどちらも `palt` 未適用。技術記事系サービスの共通傾向]

### 3.8 縦書き

該当なし。

---

## 4. Component Stylings

構造は3テーマ共通。色は §2 の役割名を参照する。

### Hover（全部品共通。人間の決定 2026-08-26「カーソルがそこにあることを分かりやすくする」）

**新しい色を作らない。`ink` を 8% 重ねて1段持ち上げ、枠線を `accent-line` にする。**
`ink` を混ぜる形なら、ダークでは明るく、ライトでは暗くなるので、どのテーマでも
同じ規則で「乗っている」と読める。テーマごとの分岐が要らない。

| 相手 | 混ぜる先 |
|---|---|
| 自分の面を持つもの（ボタン・リアクション・吹き出し） | **自分の面色**（`surface` / `accent-soft` など） |
| 面を持たないもの（あやすの本文・一覧の行） | **透かし**（`transparent` に混ぜて重ねる） |
| 臙脂の塗りのもの（Primary ボタン） | `accent-press` に差し替える |

逆にすると壊れる。自分の面を持つ要素に透かしを当てると面が消えて地が透け、
面を持たない要素に自分の面を与えると下の役割色が消える。

**当たり判定を擬似要素で広げている部品は、擬似要素のほうを光らせる。**
ボタン自身を光らせると、押せる範囲と光る範囲がずれる。

**浮かせない。** Hover で `translateY` や影を足すのは、カードもボタンも行わない
（カードは下の Cards、ボタンも同じ扱いにそろえる）。押した反応は Press の `scale` で返す。

### Buttons

**Primary**
- 背景 `accent-fill`（高さ 48px 超の全幅は `accent-fill-large`）／文字 `on-accent`／`{typography.button}`
- Padding `13px 20px`・Radius `{rounded.md}`・min-height `48px`
- Press：背景 → `accent-press`、`transform: scale(0.985)`
- **主操作（「バブる」）だけは `scale(0.97)`。** 明度の変化だけにしない
  （人間の決定 2026-08-26「押してほしいボタンには、明度の変化だけでなくアクションや動きを付ける」）。
  いちばん押してほしいボタンが、いちばん反応の薄いボタンになっていた
- **ずっと画面にあるボタンは、読みものより前に置く**
  （人間の決定 2026-08-27「常に表示されている状態を優先」）。
  「バブる」は中央の列の**中に居ない**。送っても動かないものは、読みものの順にも並ばない。
  DOM 上は左サイドの直後（＝フィードより前）に置き、見た目の位置（中央の列の右下）は
  grid の列指定で決める。列の最後に置くと、キーボードだけで操作する人は
  カードを全部通り過ぎてからしか届かない。
  焦点の順序は「ナビゲーション → 主操作 → 読みもの」になり、意味も操作性も保たれる（WCAG 2.4.3）
- **「バブる」だけ、ほかのボタンより一段大きい**（`--fab-height` = 56px。ふつうは 48px）。
  このサービスでいちばん押してほしいボタンなので、色と位置だけでなく**大きさでも**主役にする
  （人間の指示 2026-08-27「みんなに一番押して欲しいボタンです」）
- **「押す前の誘い」は、1画面にひとつだけ。** 複数入れると §7.2 と衝突する
  - タイムライン側 … 「バブる」の `bubble-call`（§7.3）。着いたときに一度だけ浮かび上がり、
    輪が2回ひろがって止まる。カーソル／フォーカスで小さなバブルが3つ立ちのぼる
  - 投稿パネル側 … 送信ボタンが、送れる状態に変わった瞬間だけ一度 pop する
    （§7.3 の `like-button` 型を `scale 1.06` / `ease-smooth-out` で使い回す）
  - **この2つは同時に画面へ出ない**（パネルが開くと「バブる」は消える）。どちらも常時は動かさない
- 園児UI：Radius `{rounded.pill}` + 2px 枠 + ずらし影

**Ghost**
- 背景 `transparent`／文字 `accent-line`／枠 `1px solid accent-line`
- Hover：背景 `accent-soft`

**Disabled**
- `opacity: 0.42` + `cursor: not-allowed`
- **S3 のお母さんボタンはここ。** 非表示にせず、ラベルに理由を書く（§0.3）

### Inputs

- 背景 `surface`／枠 `1px solid hairline`／Radius `{rounded.md}`／Padding `12px`／Font **16px**
- Focus：`outline: 2px solid accent-line` / `outline-offset: 1px`
  （枠色だけを変えない ─ 色覚に依存させない）
- `placeholder` は `muted`。愚痴を書かせる欄の placeholder を命令形にしない
  例：「なにがあった？ ぜんぶ そのままで いいよ。」
- 園児UI：枠 2px 破線

### 文字数カウンタ（150文字上限・FR-POST-002 / NFR-005）

- 入力欄の右下に `{typography.counter}` で `128 / 150`
- 残り 20 文字から `muted` → `accent-line` に変える
- **150 を超えても入力を止めない。** 超過を許して警告し、**保存ボタンを無効化する**
  → 理由：弱音を書いている途中で文字を消されるのは、このサービスで最も避けたい体験。
    仕様が禁じているのは「保存」であって「入力」ではない（FR-POST-002）
- 超過中は残り文字数を負数で出し、理由を1行添える（「150文字までにしてね。あと -12文字」）

### Cards（バブルカード）

- 背景 `surface`／枠 `1px solid hairline`／Radius `{rounded.lg}`／Padding `14px`／**影なし**
- カード間 `{spacing.sm}`
- **ホバーで浮かせない。** フィードは読み物であってボタンの列ではない
- 園児UI：枠 2px + ずらし影 `6px 6px 0`

### あやすカード（★役割色で塗らない。§2.6）

- 背景 `surface`／枠 `1px solid hairline`／Radius は**バブルと左右反転**（落とす角は左上）
- **お母さんのあやすでも、地と枠を役割色にしない。** 役割はアバターとロールバッジが示す
  → 理由：塗り面積が「お母さんのあやすに目が奪われる」の主因のひとつ（§2.6）。
    §2.5 の二重化はアバターとバッジで満たしている
- しっぽは付けない。1画面にしっぽを何本も出すと、どれが主役か読めなくなる

### Persona Badge / Avatar

- Avatar 34px 円（あやす内は 26px）／地は `*-soft`／枠 `1px solid` の役割色／中身は「赤」「母」の1文字
- Role Badge：`{typography.label}`／Radius `{rounded.pill}`／地 `*-soft`／文字は役割色
- **自動生成の図形・グラデーションを使わない**（§0.1-3）
- 園児UI：不定形の角丸 + `-2.5deg` の傾き

### リアクション行（★対象で形が変わる。§0.2）

```
バブル / 赤ちゃんあやす → [おぎゃー n] [よしよし n] [わかるわぁ n]
お母さんあやす        → [ばぶー n]
```

- 既定：枠 `1px solid hairline`／地 `surface`／文字 `muted`
- 押下済：枠・文字 `accent-line`／地 `accent-soft`／`font-weight: 700`
- 数値は `tabular-nums`（増えても幅が飛ばない）
- **`わかるわぁ` はボタン1つ。**ラベル「わかるわぁ」＋哺乳瓶アイコン。`哺乳瓶` という別ボタンを作らない
- タッチターゲットは見た目が小さくても padding で 44px を確保する
- 押した瞬間の演出は §7 の `like-button`

### AI 文章変換の3状態（FR-AI-TRANS-002〜005、FR-MOD-030）

同じ場所（入力欄の下）に、状態ごとに違うものを出す。

| 状態 | 出すもの | 出さないもの |
|---|---|---|
| 処理中 | インジケータ + 「変換しています」（NFR-006） | — |
| `allow` | 変換文のプレビュー + 「これで書く」（本文に差し込むだけ） | — |
| `rewrite_required` | 「このままでは投稿できません」+ 書き換えの促し | 変換文（使える形で出さない） |
| `block` | §0.4 の匿名性保護の説明 | **変換文・判定の内部情報** |
| AI 停止中 | 「いま、ことばのお手伝いは使えません」 | — |

- **どの状態でも本文の入力欄と保存ボタンは生かしたまま**（NFR-001/003）
- `allow` で「これで書く」を押しても**保存はしない**。本文欄に入るだけ（FR-AI-TRANS-006/007）

### 拒否バナー（モデレーション・§0.4）

- 地 `accent-soft`／文字 `accent-line`／Radius `{rounded.xs}`／`{typography.body}`
- 匿名性保護を理由として説明する。内部情報を出さない
- 入力内容を消さない。書き直せる状態で残す

### Tags

- Radius `{rounded.pill}`／地 `card`／文字 `muted`／`{typography.label}`
- **`muted` は `card` の上で 4.5:1 が必要**（§2.2 の条件2）

### Meter（赤ちゃん度・お母さん度）

- 高さ 9px／Radius `{rounded.pill}`／地 `card`／塗りは `baby` または `mother-fill`
- 数値（`2歳3ヶ月`）は `{typography.metric}`。**バーだけで伝えない**
- 園児UI：高さ 12px + 枠線

### バブルの削除（FR-POST-006/007）

- 自分のバブルにだけ出す。他人のバブルには出さない
- **不可逆なので確認を出す。** 「このバブル、消しちゃう？ 元には戻せないよ」
- 確認ダイアログを使うのはここだけ。可逆な操作に確認を出すと、押し流す癖がつく
  [出典: apple-design ─ Agency。取り消せる操作は confirm を出さず、不可逆な操作にだけ出す]

### Empty State

- `2px dashed hairline`／Radius `{rounded.lg}`／中央揃え
- コピーは責めない。「ここから先はまだ、だれも吐き出していません。いちばん最初になってみる？」
- 園児UI：ここが一番装飾を効かせる場所

### Note Box（仕様の説明）

- 地 `accent-soft`／文字 `accent-line`／Radius `{rounded.xs}`／`{typography.label}`
- 「バブルはいつでも赤ちゃん」「この画面はあなたにしか見えません」に使う
- 投稿ガイドライン（FR-PRIV-001）もここに置く

---

## 5. Layout Principles

### 5.1 Spacing Scale

| Token | Value | 用途 |
|---|---|---|
| xxs | 4px | アイコンと文字の間 |
| xs | 8px | タグ間、リアクション間 |
| sm | 12px | カード間、要素の縦間 |
| md | 16px | 画面左右の余白、カード内 padding |
| lg | 24px | セクション間 |
| xl | 32px | 画面上下の余白 |
| xxl | 48px | 空状態の上下 |

### 5.2 Container

- **フィード最大幅 600px。** 1行 40 全角文字前後で、日本語の可読幅に収まる
- 画面左右 padding 16px ／ タッチターゲット最小 **44 × 44px**
- MVP は1カラム。デスクトップでもフィードは 600px 固定で、余白を広げるだけ。
  **カラムを増やさない**（フィードを2列にすると、バブルを「流し読みする対象」に変えてしまう）

### 5.3 視覚調整の3原則

トークンは出発点で、最後は目で合わせる。以下は**トークン値から ±2px 以内**で許す調整。

1. **上下の余白は等しくしない。** 数値的に上下同値だと、人の目は要素が下寄りに見える（上の距離の錯視）。
   ロゴ・アイコン・カード見出しの上 padding を下の **1.05〜1.08 倍**にする。
   [出典: speakerdeck 原則1。Ameba 20周年サイトの実例は上 317px / 下 295px ≒ 1.075 倍]
2. **面積が変われば色も変える。** 同じ色でも面積が大きいほど明るく・派手に見える。
   大きい塗りには `accent-fill-large`（少し暗い値）を使う。
   [出典: speakerdeck 原則2 ─ 色の面積効果。Ameba は `#237B31` と `#298737` を面積で使い分けている]
3. **数値ではなく視覚的な重さで揃える。** 同じ `gap` でも、文字量の多いカードと少ないカードでは
   偏って見える。密度で微調整する。調整が ±2px を超えるなら、**トークン自体が間違っている**と考える。
   [出典: speakerdeck 原則3]

---

## 6. Depth & Elevation

**色面優先、影は例外。** 深さは「面色の段差」と「1px の線」で作る。
影を使うのは、面から浮いていることが機能的に意味を持つ2か所（モーダル・トースト）だけ。
[出典: claude DESIGN.md ─ elevation philosophy is "color-block first, shadow rare"]

| Level | ノーマル | ダーク | 園児UI |
|---|---|---|---|
| 0 | なし | なし | なし |
| 1 | `1px solid hairline` | `1px solid hairline` または面色差 | `2px solid`（実線／破線） |
| 2 | 地を `card` に沈める | 地を `card` に**上げる** | 地を `card`（黄）にする |
| 3 | `0 12px 32px -12px rgba(49,38,36,.18)` | 影は効かない → **面色差で代替** | `6px 6px 0`（ずらし影） |

- **ダークで影は機能しない。** 暗い地の上の暗い影は見えない。階層は面色を「明るくする」方向で作る
- `backdrop-filter` は使わない。Android 中位機で描画コストが跳ね、
  `prefers-reduced-transparency` の分岐も増える。3テーマ × 半透明の検証はハッカソンのコストに見合わない

---

## 7. Motion

### 7.1 Motion Tokens

transitions.dev の共有スケールをそのまま採る（プロジェクト独自の数値を作らない）。**3テーマ共通。**
[出典: transitions.dev `skills/transitions-dev/_root.css`]

```css
:root {
  --duration-stagger: 40ms;       /* 1項目ごとのずらし */
  --duration-micro: 80ms;         /* ツールチップの待ち、パスの遅延 */
  --duration-quick: 150ms;        /* モーダルを閉じる、文字入れ替え */
  --duration-fast: 250ms;         /* 開く、タブ移動、ページ遷移 */
  --duration-medium: 350ms;       /* パネル/トーストを閉じる */
  --duration-slow: 400ms;         /* パネルを開く、スケルトン→本体 */
  --duration-very-slow: 500ms;    /* 強調（リアクションの反応、文字の立ち上がり） */

  --ease-smooth-out: cubic-bezier(0.22, 1, 0.36, 1);  /* 既定 */
  --ease-in-out: ease-in-out;                          /* 入れ替え系 */
  --ease-bounce: cubic-bezier(0.34, 1.36, 0.64, 1);   /* リアクションの入場のみ */
  --ease-linear: linear;                               /* スケルトンの脈動 */

  --distance-micro: 4px; --distance-base: 8px; --distance-medium: 12px;
  --scale-modal: 0.96;
  --blur-small: 2px; --blur-medium: 3px;
}
```

### 7.2 使うときのルール

- **開閉は非対称。** 開く 250ms／閉じる 150ms。開くのは招待、閉じるのは退場
- **対称にする例外**（両方向 250ms・同じイージング）：タブ切替、アコーディオン、アイコン差し替え、
  ページ横スライド。ひとつの可逆運動として読まれるものは分けない
- **オーバーシュートは入場だけ。** `--ease-bounce` はリアクションを押した瞬間だけ。閉じるものを跳ねさせない
- **スタガーの合計を 300ms 未満に。** `40ms × 項目数`。長いリストはずらす項目数を打ち切る
- **閉じる動作に delay を入れない。** 遅く感じるなら delay ではなく duration を削る
- **数値ではなく用途で決める。** 「モーダルを閉じる」なら 300ms でも `--duration-quick` に寄せる。
  用途がどのトークンにも当たらない動きは、そのまま放置してトークン化しない
  [出典: transitions-polish SKILL.md「match on usage, never on the nearest number」]
- **テーマ切替そのものは、ゆっくり動かさない。** 明暗の急変は避けるが、色の transition は
  120ms 程度に留める。長い cross-fade は「壊れた」ように見える

### 7.3 MVP で入れる動き（6つだけ）

| 場所 | 使う型 | 理由 |
|---|---|---|
| フィード読み込み | `skeleton-reveal`（脈動 → cross-blur で本体へ） | 空白より不安が少ない |
| リアクション押下 | `like-button`（pop + 粒子） | 「受け止められた」実感の中心 |
| 投稿完了 | `toast`（下から 400ms、消えるのは 350ms） | 完了を画面遷移で伝えなくて済む |
| S5 あやすモーダル | `modal`（`scale(0.96)` → 1、開 250 / 閉 150） | 起点から出て起点へ帰す |
| 空状態のコピー | `texts-reveal`（12px + blur 3px を 40ms ずらし） | 静かな画面に呼吸を作る |
| 「バブる」 | `bubble-call`（登場 → 輪が2回 → hover/focus で3つ立ちのぼる） | いちばん押してほしいボタンだと、色と位置だけでなく**動き**でも分かるようにする（人間の指示 2026-08-27） |

**入れないもの**：`card-tilt`（読み物カードを傾ける理由がない）、`spinning-counter`（リアクション数は
`number-pop-in` で足りる）、`shimmer-text`、`plus-menu-morph`、`avatar-group-hover`（フォロワーを
並べる画面が存在しない ─ §0.5）。

### 7.4 反応の作法

[出典: emilkowalski/skills `apple-design`]

- **押した瞬間に反応する。** `:active` で `transform: scale(0.97)` を 100ms。
  `click` を待って初めて見た目が変わる UI は死んでいるように感じる
- **出た経路で帰る。** 下から出たモーダルは下へ帰す。右から出たパネルは右へ帰す
- **起点に紐づける。** `transform-origin` を、開くきっかけになった要素の側に置く
- **`transition: all` を書かない。** 動かすプロパティを列挙する。動かすのは `transform` と `opacity` を基本にする
- **フィードバックは4種類**（状態・完了・警告・エラー）を区別する。検証はフォーム送信時ではなくその場で返す

### 7.5 Reduced Motion（必須）

reduced motion は「動きを消す」ことではなく「**前庭系に触らない等価物に置き換える**」こと。
スライドと跳ねを止め、`opacity` と色の変化は残す。

**`* { animation-duration: 1ms }` で全部止めない。** それだと `opacity` の変化まで消えて、
「置き換える」ではなく「消す」になる。代わりに、**移動・拡大・跳ね・ぼかしの原資に
なっているトークンだけを 0 と 1 に潰す**（2026-08-26、実装に合わせて改訂）。

```css
@media (prefers-reduced-motion: reduce) {
  :root {
    --distance-micro: 0px;
    --distance-base: 0px;
    --distance-medium: 0px;
    --scale-modal: 1;
    --blur-small: 0px;
    --blur-medium: 0px;
    --press-scale: 1;
    --press-scale-strong: 1;
    --pop-scale: 1;
    --ease-bounce: var(--ease-smooth-out);
  }

  /* 等価物が無いものだけ、個別に消す */
  .eg-burst { display: none; }
}
```

こうしておくと、§7.3 の型を使い回すかぎり**新しく足した動きも自動的に安全側へ倒れる**。
`transform` や `filter` の値をトークン経由にしていない動きだけ、個別に手当てする。

---

## 8. Responsive Behavior

### 8.1 Breakpoints

| Name | Width | 挙動 |
|---|---|---|
| Mobile | < 600px | **既定。** 1カラム、左右 16px、下部ナビ |
| Tablet | 600–959px | フィード 600px 中央寄せ、下部ナビ維持 |
| Desktop | ≥ 960px | フィード 600px 中央寄せ + 左に固定ナビ 220px。フィードは広げない |

**モバイルファーストで書く。** メディアクエリは `min-width` で足していく。
深夜に片手で開かれる前提なので、主要アクションは画面下 1/3 に置く。

### 8.2 タッチターゲット・文字サイズ

- 最小 44 × 44px。リアクションは見た目が小さくても padding で確保する
- 下部ナビは `env(safe-area-inset-bottom)` を足す
- 本文は縮めない。モバイルでもバブル本文 16px / 行間 1.9 を維持する
- Display はモバイルで 24px → 20px まで。`clamp()` を本文には掛けない
- 入力欄は 16px 未満にしない（iOS Safari が自動拡大してレイアウトが崩れる）

### 8.3 テーマの切り替え（実装契約）

3テーマを持つので、切替の仕組みを最初から決めておく。

```css
/* 1. 既定（ノーマル）は素の :root に全トークンを置く */
:root { --canvas: #FFF8F1; /* … 全役割 … */ }

/* 2. OS 設定のダークは、明示指定が無いときだけ効かせる */
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="normal"]):not([data-theme="kid"]) { --canvas: #1A1513; /* … */ }
}

/* 3. 明示指定は常に勝つ */
:root[data-theme="dark"] { --canvas: #1A1513; /* … */ }
:root[data-theme="kid"]  { --canvas: #FFF9E3; /* … */ }
```

- **色は必ず変数経由。** `.card { background: #FFFDF9 }` と直書きするとテーマが増やせない
- **メディアクエリや `[data-theme]` の中だけで定義される色を作らない。**
  素の `:root` に全役割の既定値を置いてから、テーマで上書きする
- 選択は `localStorage` に保存し、`<html data-theme>` に反映する。
  既定は「OS 設定に従う」（`data-theme` 属性なし）
- 園児UI は OS 設定から自動選択されない。**利用者が明示的に選ぶテーマ**
- `prefers-contrast: more` では、各テーマの `hairline` を `body` 寄りに寄せ、`muted` を `body` に寄せる

---

## 9. Agent Prompt Guide

### 9.1 クイックリファレンス

```
用語:  投稿=バブル / コメント=あやす
上限:  バブル本文 150文字（超過は入力を止めず、保存を止める）
リアクション: バブル・赤ちゃんあやす = おぎゃー/よしよし/わかるわぁ の3種
              お母さんあやす        = ばぶー の1種のみ
              「わかるわぁ」と「哺乳瓶」は同一。ボタンは1つ

テーマ:  normal（配色検討中） / dark / kid の3つ。骨格共通・色だけ差し替え
参照:    色は必ず役割名（--canvas, --accent-line, --accent-fill …）経由

Display: "Zen Maru Gothic", "Hiragino Maru Gothic ProN", sans-serif / 700
Body:    -apple-system, system-ui, "Hiragino Sans", Meiryo, sans-serif / 400
バブル本文: 16px / line-height 1.9 / letter-spacing normal / palt なし
見出し:     0.02em + palt / 12px ラベルだけ 0.04em

Radius: 4 / 6 / 10 / 16 / 999
影: カードには付けない。モーダルとトーストだけ。ダークでは影の代わりに面色差
開く 250ms / 閉じる 150ms / ease cubic-bezier(0.22, 1, 0.36, 1)
```

### 9.2 発注の型：形容詞ではなくオブジェクトで書く

「かっこよく」「いい感じに」「おしゃれに」と書かない。AI は形容詞の意味を勝手に補完して、
どこかで見た無難な真ん中を返す。代わりに**9項目**を埋める。
[出典: X @kimuai08「落合陽一のAIデザイン術」原理1 ─ 形容詞をオブジェクトと関係に置き換える]

1. 主役となる要素（1つだけ）
2. 周辺に置く要素
3. 位置関係
4. 状態（未読／押下済／空／エラー／AI停止中／文字数超過）
5. 面の質感
6. 視線が動く順序
7. 使う人の状態（深夜・片手・疲弊）
8. 見られる媒体（375px のスマホ・暗い部屋）＋**どのテーマか**
9. **禁止する要素**

**悪い例**

```
えんじいろのタイムラインをかっこよく作って。
```

**良い例**

```
DESIGN.md に従って S2 タイムラインを実装してください。テーマは normal と dark の2つ。

- 主役：バブルカード1枚（本文16px/1.9）。1画面に2.5枚見える密度
- 周辺：上に「おなじくらい つかれてる子」の帯、下にリアクション
- 視線：ニックネーム → 本文 → リアクション の順
- 状態：読み込み中（skeleton）／空／並んだあと の3つ（「よんだ／まだ」は持たない）
- リアクションはバブル対象なので おぎゃー/よしよし/わかるわぁ の3種のみ
- 使う人：深夜、片手、暗い部屋。主要アクションは画面下 1/3
- 媒体：375px のモバイル既定。デスクトップでもフィードは600pxのまま
- 禁止：影（カードに付けない）／ホバーで浮かせる／フォロワー数／通報／
        自動生成アバター／絶対時刻／本文の自動リンク化／2つ以上の主役／
        色の直書き（必ず --canvas 等の役割名経由）
```

### 9.3 監査の型：作った AI に採点させない

作った AI に自己採点させると必ず甘くなる。**別のセッション・別の役割**で監査する。
[出典: 同記事 原理7 ─ 制作と採点を分ける／STEP6 ─ 却下理由を次の生成条件にする]

```
あなたは厳しいクリエイティブディレクターです。以下の実装を DESIGN.md と
docs/specification.md に照らして監査してください。褒めなくてよいです。

各項目を10点で採点：
目的との一致／情報の優先順位／視線誘導／余白／可読性／
トークン遵守／媒体への適合／独自性／説明可能性／実装可能性

そのうえで答えてください：
1. 主役は1つに絞られているか
2. 装飾を30%削っても成立するか
3. 色をなくしても情報の階層が分かるか
4. DESIGN.md §0 の Non-Negotiables を破っていないか
   （ペルソナ非連結／対象別リアクション／あやすの返信ペルソナ／
     モデレーション拒否の表示／存在させないUI／AI結果の自動保存）
5. トークンにない生の数値（px / ms / hex）が残っていないか。色の直書きは無いか
6. 3テーマそれぞれでコントラスト比が規約を割っていないか
7. prefers-reduced-motion の分岐はあるか
8. 375px と 960px の両方で崩れないか
9. 破綻（直すべきバグ）／逸脱（意図と違うが個性になる）／発見（採用したい）に3分類せよ
10. 次回の発注条件に追加すべき「却下理由」は何か

80点未満の項目があれば修正し、修正前・問題・修正内容・修正理由を記録してください。
```

### 9.4 AI の想定外を3分類する

生成物の「間違い」を全部潰さない。3つに分けて扱う。
[出典: 同記事 原理4]

- **破綻**：文字化け、崩れたレイアウト、AA 割れ、§0 違反 → **直す**
- **逸脱**：意図と違うが、えんじいろらしさになりうる → **Issue（`ai-discussion`）に置く**
- **発見**：中心に昇格させたい要素 → **このファイルに取り込む**

### 9.5 却下理由を残す

うまくいった理由は再現しにくいが、却下した理由は次の発注条件として確実に効く。
PR 本文に、採用した案と**捨てた案とその理由**を1行ずつ書く。§10 がその蓄積場所。

---

## 10. 取捨選択の記録（Adopted / Rejected）

**真似すべきは作風ではなく制作構造。**
[出典: X @kimuai08 記事の結論]

### 10.1 方向性の決め方（経緯）

1. 3つの方向性（A 和紙 / B 園児の画用紙 / C 色シール）を S2・S3・S8 で実際に描いて比較した。
   見本：<https://claude.ai/code/artifact/e78f04b9-7eef-46f4-9f89-650c9527e2ef>
2. 当初は「A を土台、B・C を部分採用」という1本の方向に絞る結論を出した。
3. **人間の決定により、3テーマ（ノーマル／ダーク／園児UI）を並立させる方針に変更。**
   A は**ノーマル**の出発点、B は**園児UI** の出発点として残り、C は見送り。
   ダークは3案の探索に含まれておらず、§2.3 で新たに設計した。
4. **`frontend/color-check` の UI は参考にしない。** あれは色だけを決めるための使い捨てページで、
   画面設計の参考として作られていない。**色の検討の場としてのみ**扱う。

### 10.2 採用したもの

| 出典 | 採ったもの | どこに効いているか |
|---|---|---|
| **VoltAgent/awesome-design-md** | DESIGN.md という形式（9セクション + YAML トークン、見出しは英語） | このファイルの構造 |
| **claude DESIGN.md** | 生成りの canvas + 単一アクセント／影より色面で階層 | §2.2, §6 |
| **kzhrknt/awesome-design-md-jp** | 日本語タイポグラフィのセクション設計（3.1〜3.8）とフォールバック必須ルール | §3 全体 |
| **note DESIGN.md** | 本文の行間で可読性を確保し、`letter-spacing` と `palt` は見出し専用にする | §3.5, §3.7 |
| **zenn DESIGN.md** | システムフォント先頭のスタック／技術系は `palt` 未適用／`#000` を使わない | §3.1, §3.3, §2.5 |
| **hobonichi DESIGN.md** | 内容ごとの淡色で分類する考え方（園児UI の淡い面に限定採用） | §2.4 |
| **transitions.dev** | モーションの共有トークンと5つの型 | §7.1, §7.3 |
| **transitions-polish** | 開閉の非対称・対称の例外・スタガー合計 300ms・「用途で決める」原則 | §7.2 |
| **emilkowalski/apple-design** | pointer-down で即反応／出た経路で帰す／`transform-origin` を起点に／reduced-motion は等価物に置換／tracking はサイズ別／フィードバック4種／**不可逆な操作にだけ confirm を出す** | §3.5, §4 削除, §7.4, §7.5 |
| **speakerdeck（原 雄一）** | 光学的中央（上余白 1.05〜1.08 倍）／色の面積効果／視覚的な重さで揃える | §5.3 |
| **X @kimuai08（落合式）** | 形容詞→オブジェクト9項目／制作と採点の分離／誤生成の3分類／却下理由の蓄積／媒体まで設計 | §9 全体 |
| **X @mmmsumo（Claude Design）** | 「形式（デザインシステム）を先に固め、内容は別で練る」順序 | このファイルの存在理由 |

### 10.3 見送ったもの（理由つき）

| 出典 | 見送ったもの | 理由 |
|---|---|---|
| **C 案 色シール** | ブランド色を固定せず、カードの地色を分類記号にする | 「えんじ（臙脂）いろ」という名前の由来が薄まる。色数が増えるとペルソナの役割色（§0.1-2）が埋もれ、非連結の可読性が落ちる。淡色の使い方だけ園児UI に残した |
| **MengTo/threeui** | Three.js / WebGL のシェーダー hero | 本音吐露 SNS のトップに派手な hero は体験と逆行する。バンドルとバッテリー負荷、フロント技術未定。→ **1つだけ持ち帰り**：見た目のつまみを `headingFont / bodyFont / primaryColor / headingSize / headingLetterSpacing` の5つに固定して比較する設計は、配色検討の道具を作るときに使える |
| **apple-design（動きの物理）** | spring、velocity handoff、momentum projection、rubber-banding | ドラッグ・スワイプ主体の UI を MVP に持たない。CSS だけで完結させたい。スワイプで閉じる要求が出たら、そのとき §7 に足す |
| **apple-design（素材）** | `backdrop-filter` の半透明チローム | Android 中位機で描画コストが跳ね、`prefers-reduced-transparency` の分岐が増える。3テーマ × 半透明の検証は割に合わない |
| **落合式 原理2** | カメラマンの言葉（レンズ・絞り・光）で指示する | 画像生成を行わない |
| **落合式 原理5（一部）** | スクロールで意味が変わる、背景がマウスに反応する演出 | ノーマルの「静かな編集面」と衝突する。認識の変化を時間で設計する考え方自体は S3 →完了トースト→ S2 に適用済み |
| **Claude Design（記事のワークフロー）** | スライド自動生成 | プロダクト UI には使わない。**ハッカソン最終発表のスライド**を作るときに使う（このファイルをデザインシステムとして読ませられる） |
| **transitions.dev（22個）** | card-tilt / spinning-counter / shimmer-text / plus-menu-morph / avatar-group-hover ほか | 対応する画面が存在しない、または方針と衝突する。§7.3 の5つで足りる |
| **`frontend/color-check` の画面設計** | あのページのレイアウト・コンポーネント | 色を決めるための使い捨てページで、画面設計の参考として作られていない（§10.1-4） |

---

## 11. Known Gaps

- **ノーマルの配色が未確定**（§2.2）。決める条件は5つ挙げてあるので、案はその条件で判定する
- **園児UI の「本文だけは 4.5:1 を守る」を採るか**（§2.4）。人間の決定が必要
- **`Zen Maru Gothic` の読み込み方法が未確定。** Google Fonts の `<link>` かセルフホストか。
  フロントの技術選定と同時に決める
- **アイコンセットが未定。** 現状は文字（赤／母／＋）で代替。`わかるわぁ` の哺乳瓶アイコンは要作成
- **スタンプ（本文に挿入する画像）の見た目が未定。** `GET /api/stamps` のカタログ表示と、
  本文中でのインライン表示サイズ
- **エラー表示の共通形式が未確定**（backend のエラーレスポンス形式そのものが未確定 / Issue #8）
- **通知の置き場所が未定。** 画面一覧（S1〜S8）に通知画面が無く、トーストで足りるかは要確認
- **ログイン画面が画面一覧に無い**（design_doc §10 / Issue #7）。追加されたらこのファイルにも足す
- **AI 変換の待ち時間の実測がまだ。** `skeleton` かボタン内インジケータかは実測後に決める

---

## 12. このファイルの更新

- 値を変えるときは、`[出典: …]` と §10 を先に読む。元の値には理由がある
- **`docs/specification.md` が変わったら §0 と §4 を必ず見直す。**
  リアクションの種類・文字数上限・モデレーションの表示は、そこ由来の制約
- 実装しながら発見したことは、コードに書いて終わりにせず §10 に1行足す
- 上位文書（`AGENTS.md` / `docs/specification.md` / `docs/design_doc.md`）と矛盾したら**上位が正**
- 決められないことは仮定しない。2案以上の利点・欠点と推奨案を示して Issue で相談する（`AGENTS.md`）
